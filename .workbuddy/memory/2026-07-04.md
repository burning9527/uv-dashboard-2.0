# 2026-07-04 工作日志

## 26暑系统班学员信息表 — 余露老师班级学员填充

- **数据源**: `~/Downloads/招生明细-默认展示剔除退费的订单..._20260704_200722.xlsx`（招生明细，49列）
- **模板**: `~/Downloads/26暑系统班学员信息表.xlsx`（含14个sheet，每个老师一个sheet，余露sheet有报名信息/电访信息/讲次反馈三大区块）
- **输出**: `26暑系统班学员信息表_余露.xlsx`（项目根目录）
- **学员数**: 22人（全部在读、双语素养、潜能班）
- **字段映射**: A姓名→学生姓名, B年级, C授课老师→主讲(去编号), D校区→教学点(去"教学点"后缀), E学科, F班型, G暑假期次→期次, H上课时段, I顾问→订单归属顾问(为空时用课程跟进顾问), J是否续秋(留空), K是否新生(业务归属=拉新→是, 续报/续报扩科→否)
- **排序**: 期次 → 校区 → 上课时段 → 年级 → 姓名
- **格式保留**: 原模板合并单元格、边框、字体、对齐方式全部保留；L-AE列(电访/讲次区)保留空白格边框供手动填写

## 26暑系统班学员信息表 — 全老师填充

- **输出**: `26暑系统班学员信息表_全老师.xlsx`（项目根目录）
- **学员总数**: 462名，17位老师
- **模板已有sheet(11位)**: 余露22人、邓慧敏33人、爱西43人、庄蕙霞31人、郑皓月25人、宗志鹏21人、韩舒波40人、王璇47人、徐康37人、黄铭怡24人、陈南10人
- **新建sheet(6位)**: 刘佳明41人、刘希子7人、广州主讲22人、肖锐5人、赖浩轩43人、高心怡11人
- **教学点**: 新宝利大厦、同创汇、天成大厦（已去"教学点"后缀）
- **主讲名清洗规则**: 去尾部编号(余露09→余露, 王璇36→王璇等)；爱西瓦克将阿努尔→爱西；广州主讲01→广州主讲
- **学科类型**: 双语素养、雪球思维两种；班型：潜能班、源能班两种

## UV Dashboard 矩阵/总览优化
- **矩阵合计续报率**: `db.py get_period_teaching_point_matrix` 给 `point_totals`/`period_totals` 增加 `renewal_rate`；`index.html totalHTML` 显示 badge
- **学科续报率分期次**: `app.py api_overview_stats` 新增 `renewal_rate_by_period`；`index.html` 各学科续报率卡片底部用 `.hint` 展示零期/一期/二期/三期分次续报率

## UV Dashboard 学科续报率卡片视觉优化
- **布局**: 学科续报率卡片（数学/语文/英语）采用 `side-hints` 类，内容横向 flex 布局：左侧主数字，右侧分次续报率纵向四行排列
- **样式**: `.stat-card.side-hints` 增加 `display:flex`、`align-items:center`、`justify-content:space-between`，小字 `11px` 右对齐，每行独立显示零期/一期/二期/三期
- **服务**: 已重启 Flask（端口 5200），`node --check` 语法检查通过

## UV Dashboard 业务趋势图表优化
- **时间轴按天聚合**: `app.py /api/trends/data` 同一天多次校准取最后一次快照
- **三线合一**: 在读/退费/续费合并为一张图表 `chartOverview`，`interaction.mode:'index'` 悬停联动
- **图表精简**: 从 6 张缩减为 4 张（全景三线 + 新增学员 + 分学科 + 分教学点）

## UV Dashboard 分享卡片功能
- **后端 API**: `/api/share-card` 支持 period/teaching_point/subject/teacher 四维筛选
  - 返回 kpi(活跃PV/UV/续报率/续费PV/退费PV/班级数) + by_point(分教学点表格) + by_subject(分学科) + by_teacher(老师排行)
  - 续报率颜色分档: ≥60%绿 / 50-60%蓝 / 45-50%草绿 / <45%琥珀
- **前端**: 矩阵板块添加"分享卡片"按钮，弹出模态面板
  - 左侧: 5组维度选择器（期次/教学点/学科/老师/卡片指标），chip 风格与矩阵一致
  - 右侧: 实时预览卡片，含 KPI 区 + 教学点表格 + 学科卡片 + 老师排行表（姓名/在读/已续报/进度条/续报率）
  - `html2canvas@1.4.1` 导出 PNG，scale=2 高清
- **CDN**: 新增 `html2canvas.min.js`（jsdelivr），与 Chart.js 并列

## UV Dashboard 分享卡片优化（第二轮）
- **圆角+阴影**: 卡片外层 wrapper `padding:24px;background:transparent`，内层 `border-radius:16px;box-shadow:0 8px 32px rgba(0,0,0,0.12)`；html2canvas `backgroundColor:null` 保留透明背景使阴影可见
- **老师/教学点 chip**: 改为从 `/api/share-card` 响应的 `available_teachers`/`available_points` 字段获取（不受当前筛选影响），替代之前 `/api/overview/filters` 单独请求
- **卡片指标 UI**: 移除 `share-chips-vert` 垂直布局和 checkbox，改为与期次/学科等一致的水平 chip 布局，选中时显示 ✓ 前缀
- **KPI 卡片风格统一**: KPI 卡片增加彩色圆点（与分学科卡片一致），字体从 24px 调整为 20px
