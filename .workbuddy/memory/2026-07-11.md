# 2026-07-11 工作日志

## UV Dashboard 2.0 — 数据明细三处迭代（#332~#335）

### 需求
1. 学员检索 sub-tab 移到最右边
2. 顾问看板两个切换按钮位置不和谐
3. 主讲/顾问/学员检索的学员情况 UI 迭代更高级、美观大方、风格统一

### 改动（纯前端，无需重启后端；静态文件即时生效）

**1. sub-tab 顺序（templates/index.html）**
- 顺序改为：期次矩阵 → 排班看板 → 主讲看板 → 顾问看板 → 学员检索（学员检索置最右）

**2. 顾问看板切换按钮布局（uv-components.css）**
- `.board-sidebar-toggles` 由 `flex-direction: column`（纵向堆叠）改为 `row`（横向单行，可换行，右对齐），与主讲看板头部高度一致，视觉和谐

**3. 统一学员卡片 UI（高级版）**
- `uv-config.js` 新增：`SUBJECT_TINT`（三科淡色映射）、`studentInitial(name)`（取首字做头像）、`subjectChip(subject, cfText)`（学科彩色 chip + 续秋/状态附文）、`renewalBadge(continueFall, renewalClass)`（pre=开课前续/new=当期转化/已续秋/未续）、`_esc()`
- `uv-components.css`：新增 `.stu-list`/`.stu-card`（头像+姓名UID+学科chip+元信息+续秋徽章，hover 浮起）、`.subj-chip`（学科 chip，色由内联 style 控制）、`.subj-chip-muted`（未报学科）；`.badge-renew` 升级为前置圆点的高级徽章
- `uv-base.css`：`.badge-renew` 基础样式升级为 inline-flex + `::before` 圆点（全局统一，含弹窗）
- `uv-teacher.js` viewDetail：学员表格 → 统一学员卡片（含班级链接钻取）
- `uv-advisor.js` viewDetail：学员表格 → 统一学员卡片（多科 chip 聚合）
- `uv-student.js` renderTable：表格 → 统一学员卡片（三科 chip 含状态·续，未报学科置灰）

### 验证
- 4 个 JS 文件 `node --check` 全通过
- 服务（PID 93554，5200端口）实时服务新静态文件：
  - 首页 sub-tab 顺序 = matrix/schedule/teacher/advisor/student（学员检索在最右）✓
  - uv-components.css 含 stu-card ✓；uv-config.js 含 subjectChip ✓
  - teacher-detail API 返回 144 学员，字段含 renewal_class/continue_fall 等，供卡片渲染 ✓
- 浏览器刷新即可见效果；点击学员卡片 → 学员详情弹窗，点击班级链接 → 班级详情弹窗（事件委托）

## UV Dashboard 2.0 — 主讲/顾问看板独立筛选器 + 班级按期次分组（#336~#339）

### 需求
1. 顾问和主讲看板各加独立筛选栏（不复用全局 dashboard 筛选）
2. 主讲筛选器：期次、学科、教学点、班型
3. 顾问筛选器：期次、教学点
4. 主讲看板班级卡片默认全部展示，按期次排序/分组

### 后端改动（需重启 5200）
- `engine/__init__.py`：`FilterSpec` 增加 `class_types` 字段 + `from_params` 解析 `class_type`（逗号分隔）；`compute()` 在主讲筛选后增加班型过滤 `if fs.class_types: cls_type not in fs.class_types → continue`
- `api/__init__.py`：`api_overview_teacher_list` 与 `api_overview_teacher_detail` 增加 `subject`/`class_type` 参数并转发
- `repository/__init__.py`：
  - `get_teacher_detail` 增加 `subject`/`class_type` 参数（用 `_split_periods` 拆逗号做多选过滤），`teaching_point` 也改多选（`tp_list`）；班级列表排序由 `class_id` 改为 `_period_sort_key`（按 PERIOD_ORDER 最小期次索引，同期次内按 class_id）
  - `get_advisor_detail` 的 `teaching_point` 改多选（`tp_list`，之前是单值 `pt != teaching_point`）
  - `get_filter_options` 新增返回 `class_types`（从 subjects_json 的 class_type 提取）

### 前端改动（纯静态，刷新即生效）
- `templates/index.html`：`#sub-teacher` 加 section-filter-bar（ms-tb-period/subject/tp/class-type + 查询/重置）；`#sub-advisor` 加 section-filter-bar（ms-ab-period/tp + 查询/重置）。均用 `.multi-select` 组件
- `uv-filters.js`：`state` 新增 `teacherBoard`(period/subject/teaching_point/class_type) 与 `advisorBoard`(period/teaching_point) 两 section（Set）；`options` 加 `class_types`；`_msOptionsMap`/`_msStateMap` 加 ms-tb-*/ms-ab-* 映射；`buildParams` 加 teacherBoard/advisorBoard 分支（Set→逗号，空/满=全选不发参）；`renderAllDropdowns` 加两板块渲染；init 从 API 读 class_types；resetSection 走 else 分支自动清 Set
- `uv-app.js`：新增 `queryTeacherBoardFilters`/`resetTeacherBoardFilters`/`queryAdvisorBoardFilters`/`resetAdvisorBoardFilters`（reset 重渲对应下拉 + 重载）
- `uv-teacher.js`：`_params()` 改用 `buildParams('teacherBoard')`；viewDetail 班级卡片改为按期次分组渲染（`_groupClassesByPeriod` 保持后端期次排序聚合，每组 `.class-period-group` + `.cpg-head`(圆点+期次名+班数)）
- `uv-advisor.js`：`_params()` 改用 `buildParams('advisorBoard')`
- `uv-components.css`：新增 `.class-period-group`/`.cpg-head`/`.cpg-dot`/`.cpg-count` 样式

### 关键约定
- 主讲/顾问看板筛选器用**查询按钮触发**（多选下拉 onMultiSelectChange 不自动刷新），与排班看板多选下拉一致
- 班级期次分组依赖后端已按期次排序（`_period_sort_key`），前端只需按期次首次出现顺序聚合

### 验证
- py_compile（engine/api/repository）+ node --check（4 JS）全通过
- filters 返回 class_types=[启能班,潜能班]；teacher-list?subject=雪球思维 10→4；?class_type=潜能班 detail 7班→3班；teacher-detail 班级按 零期→一期→二期→三期 排序 ✓；advisor-detail period/teaching_point 多选过滤生效 ✓

## UV Dashboard 2.0 — 交叉筛选核查 + 学员检索升级 + 多选 toggle（#340~#343）

### 请求 1：交叉筛选数据核查（启能班场景）
- 结论：**当前交叉筛选数据无 bug**。穷举交叉组合 curl 验证：
  - 主讲看板 启能班+陈中帅+雪球思维：teacher-list.active=80 = teacher-detail.total_active=80 ✓
  - 顾问看板 零期+同创汇：advisor-list.active=23 = advisor-detail.uv_active=23 = student_count=23 ✓
  - dashboard stats 学科+教学点 交叉：active_uv 返回正常 ✓
- 之前截图「对不上」应源于前端旧版本（按本 session 已修复版本复现均正确），未改后端交叉算法。

### 请求 2：学员检索筛选器升级
- 原生 `<select>` 移除，改为 5 个多选下拉：教学点/期次/主讲/续报状态/在读状态（ID `ms-student-*`）
- 新增搜索框 `student-keyword`：可搜 姓名 / 学生ID(uid) / 班级ID(class_id)
- 「续秋」筛选器改称为「续报状态」，选项固定 已续费/未续费（前端显示；发给后端时映射 已续费→已续报、未续费→未续报）
- 后端 `get_filtered_students`：keyword 模糊匹配（lower）、各维度多选（`_split_periods`）、续报状态**学员维度**（已续报=至少一科已续；未续报=无任何科已续）
- 验证：keyword 李思逸→1、班级ID 5230719→23；续报 已续报84+未续报349=433，交集0、并集=全部，互斥无遗漏 ✓

### 请求 3：多选改为 toggle 语义
- `uv-filters.js` `onMultiSelectChange` 重写：点一下选中，再点一下取消
  - 当前全选 → 点某项 = 取消该项、其余保留（不再「只剩该项」）
  - 已选中 → 再点 = 取消；点其他 = 加选；加满 = 回归全选（空Set）
- Node 单测通过：[]→点零期=[一,二,三]→再点零期=[]→点一期=[零,二,三]→再点一期=[]

### 续作（本 continuation）：续报状态健壮性加固
- 发现矩阵 helper `_filter_matrix_students` 的续报逻辑仍是**学科内 per-subject**（多科学生可能被重复/错计），虽当前数据无 mixed 学生未触发，但属 latent bug
- 改为与 `get_filtered_students` 一致的**学员维度**：循环前预计算 `_renewed_any/_renewal_pre/_renewal_new`，循环内只做判定；pre/new 仍保留（学员含任一该类别科目即命中）
- 重启 5200（kill 旧 PID 2381 → nohup 新进程 PID 3006），py_compile + node --check 全通过
- 复测：teacher 80=80、advisor 23=23、renewal 84+349=433 交集0、matrix 200、日志无 error ✓

## UV Dashboard 2.0 — 教学点归属判定改为班级(教室)推导 + 学员检索迭代（#344~#349）

### 请求 1：教学点判定 bug 修复（判定问题，非筛选逻辑）
- 用户明确：之前的「交叉筛选对不上」是**判定**问题。学员归属应**默认跟随班级所在教学点（教室）**，而非学员级注册教学点。
  - 例：学生注册=新宝利，但有一节课在同创汇 → 筛同创汇应显示他，筛新宝利不应有他。
- `config/__init__.py` 新增 `student_teaching_points(subjects, fallback_pt)`：遍历各科目用 `get_room_teaching_point(room,'')` 推导教学点集合；若所有科目都无法映射（room 空/'-'），fallback 到 `normalize_teaching_point(fallback_pt)`（无学科逻辑时回退注册点）。
- 所有筛选点统一改为「任一命中即通过」：`if fs.teaching_point and not (student_pts & set(fs.teaching_point)): continue`
  - `engine.compute`（行 163）、`_filter_matrix_students`（行 580）、`get_teacher_detail`（行 914，且学科循环内按 room_pt 过滤、输出 room_pt）、`get_advisor_detail`（行 914，输出 room_pt 集合）、`get_class_schedule`（两遍均用 room_pt）、`get_filtered_students`（行 1163，并覆盖 s['teaching_point'] 为推导集合供前端展示）。
- 数据核查：8 个 room 全部映射（新宝利=教室2/3/4/5、同创汇=01智慧林/02彩虹桥/03识海阁、天成大厦=03品尚），仅 `'-'`(7470科)未映射→回退注册点。**331 名学员班级推导归属跨 >1 个教学点**，fix 后正确归位。
- 验证（合成用例）：注册新宝利+唯一课同创汇 → 同创汇命中=True、新宝利=False ✓；跨点学员两处都命中 ✓。

### 请求 2：学员检索 UI 迭代 + 筛选器
- 搜索框 `#student-keyword` 移到**最右**（`sf-group-search` 加 `margin-left:auto`，在 6 个下拉之后、搜索/重置按钮之前）。
- 「续报状态」筛选器还原为四选项：**全部（空Set）/未续报/开课前续报/当期转化**（`uv-filters.js` `ms-student-renewal` 选项 + `optionSources` 同步；`uv-student.js` 映射兼容旧值 已续费→已续报 透传其余）。
- 新增「**学科**」多选下拉 `ms-student-subject`（选项 `UVConfig.SUBJECTS`），`uv-filters.js` state/options/stateMap/buildParams/renderAllDropdowns 全接上；后端 `get_filtered_students` 新增 `subject_keys` 参数（`_split_periods(filters.get('subject'))`），任一科目命中即通过。
- 验证：同创汇 136、雪球思维+未续报 349、开课前续报 63、当期转化 32、已续报 84（63+32=95≥84，跨科允许重叠）、同创汇+雪球思维+未续报 97（≤136 且 ≤349，交叉一致）✓

### 运维坑（重要，下次必看）
- **curl 走代理**：环境有 `http_proxy=http://127.0.0.1:60275`，curl 直连 localhost:5200 会 502。联调须 `curl --noproxy localhost` 或 Python `urllib.request.ProxyHandler({})`。
- **后台进程不跨工具调用存活**：Bash 工具里的 `nohup ... &` 进程在该调用结束后被杀；测 API 须与启动在**同一调用**，或改用 `run_in_background=true` 启动服务器再单独调用测试。
- 启动 5200：开发模式数据目录 = `os.path.dirname(BASE_DIR)/uv-dashboard` = `.../uv-dashboard/uv_dashboard.db`（与 1.0 共享）；打包模式 = `~/Library/Application Support/UV Dashboard/`。

## 20:15 2.0 主讲/顾问看板 UX 修复

### 请求 1：左侧无学员的卡片不应显示
根因：`engine.compute` 在学员级教学点筛选通过后，per-subject 统计没有再按 room 推导的教学点过滤，导致某老师在筛选教学点下被计数，但详情里因 per-subject 过滤而无学员（如截图何咏04 左侧 active=1、右侧为空）。
修复：`engine/__init__.py` per-subject 循环内增加 `if fs.teaching_point and pt not in fs.teaching_point: continue`，确保 by_teacher/by_advisor 只统计真实落在目标教学点的科目。
兜底：前端 `uv-teacher.js` / `uv-advisor.js` 的 `render()` 在排序前先过滤掉当前维度 `active <= 0` 的条目。
验证：同创汇+启能班 teacher-list 现在只返回 1 位老师（陈中帅，active=36），何咏04 不再出现；同创汇 advisor-list 返回 12 位顾问且 active 均 > 0。

### 请求 2：详情页 banner 下滑吸顶
实现：将 `board-detail-head` + `board-summary`（顾问含 `board-summary-secondary`）包入 `<div class="board-detail-sticky">`；CSS 设置 `position: sticky; top: 0; z-index: 20; background: var(--bg-panel)`，在 `board-detail` 滚动区域内保持姓名与汇总数据可见。

### 请求 3：左侧卡片选中提示
实现：新增 `UVTeacher.selectedTeacher` / `UVAdvisor.selectedAdvisor`；`viewDetail()` 时更新状态并重新 `render()`，给当前卡片加 `rank-item active` 类；CSS 加高亮边框/背景。

### 请求 4：学员列表已续报排前面
实现：
- `uv-teacher.js` 的 `viewDetail()` 中 `students.sort((a,b) => bRen - aRen)`，按 `UVConfig.isContinueFall(continue_fall)` 降序。
- `uv-advisor.js` 中 `students.sort((a,b) => b.is_renewed - a.is_renewed)` 降序。

## 20:45 撤销 banner 吸顶功能（用户嫌丑）
- 用户反馈吸顶窄条「太丑」，要求撤销。
- `uv-teacher.js` / `uv-advisor.js`：移除 `.board-detail-sticky` 包裹层、`.bdh-metrics` 窄条指标块、`container.onscroll` 滚动阴影监听，详情头部恢复为常规流 `.board-detail-head`（仅标题）。
- `uv-components.css`：删除 `.board-detail-sticky`（position:sticky/top/z-index/is-stuck 阴影）与 `.bdh-metrics/.bdh-metric` 样式，`.board-detail-head` 恢复 margin-bottom:4px。
- 全局 grep 确认 board-detail-sticky/bdh-metrics/is-stuck/onscroll 无残留；node --check 通过；5200 服务 200。

## 20:50 学员列表卡片重设计（去掉右侧 UV 双 pill，三科状态卡）
- 用户要求：左侧基础信息保留；中间三科分别展示在读/未报/退费 + 续报状态；去掉最右侧不是 UV 逻辑的续报/在读双 pill；风格不变、数据更清晰、配色高级大方。
- `uv-config.js`：新增 `subjectStatusCards(data)` 通用 helper，支持对象/数组两种输入；每科渲染为独立状态卡：顶部学科色条、中间状态大字、底部续报小标签；未报学科灰色占位；退费状态红色高亮。
- `uv-student.js`：学员检索列表改用 `subjectStatusCards(s.subjects)`，移除 `subjChips` 与右侧 `_statusBlock`。
- `uv-teacher.js`：主讲看板学员列表改用 `subjectStatusCards([{subject,status,...}])`；班级链接从 line2 移入 line3「班级」项。
- `uv-advisor.js`：顾问看板学员列表改用 `subjectStatusCards(s.subjects)`，移除 `subjChips` 与右侧 `statusBlock`。
- `uv-components.css`：删除旧 `.stu-status`/`.stu-pill` 右侧双 pill 样式，新增 `.stu-subjects`/`.subj-status-card`/`.ssc-head`/`.ssc-body`/`.ssc-state`/`.ssc-renewal`/`.ssc-empty`/`.ssc-refund` 三科状态卡样式。
- 验证：node --check 4 个 JS 全通过；5200 服务 200。

## 21:00 三科状态卡二次收窄 + 减少右侧留白
- 用户反馈卡片偏宽、右侧留白多、不够和谐。
- `uv-components.css` 调整：`.stu-subjects` 从 grid 三列改为 flex 左对齐；`.subj-status-card` 固定宽度 68px；缩小 head/body/state/renewal 的字号与内边距；`gap` 从 8px 缩到 6px；整体更紧凑、右侧留白自然减少。
- 验证：node --check 全通过；5200 服务 200。

## 21:05 三科状态卡右移 + 整体收窄
- 用户要求：三科状态卡统一移到右侧，左侧只留姓名和基础详情，整体卡片宽度收窄。
- `uv-student.js` / `uv-teacher.js` / `uv-advisor.js`：将 `subjectStatusCards` 从 `.stu-main` 内移到 `.stu-right` 区域。
- `uv-components.css`：
  - `.stu-card` 改为 `justify-content: space-between`，让左侧信息与右侧三科卡分居两边
  - 缩窄卡片内边距 `10px 14px`、头像 36px、信息间距收紧
  - `.stu-right` 加 `margin-left:8px` 承载三科卡
  - `.stu-subjects` 去掉 `margin-top`，改为 `align-items:center`，三科卡垂直居中
- 验证：node --check 4 个 JS 全通过；5200 服务 200。

## 21:13 校准老师板块三科信息 + 统一未在读灰色风格
- 用户反馈：
  1. 老师看板学员三科报读信息不准（只显示了老师任课科目）。
  2. 学员检索中已报名但不在读的科目应标记为灰色，与顾问看板风格一致。
- 修复：
  - `repository/__init__.py` 的 `get_teacher_detail`：给每个学员记录附加完整 `subjects` 字典。
  - `uv-teacher.js`：`subjectStatusCards` 入参改为 `s.subjects`，确保三科均按真实报读状态渲染。
  - `uv-config.js` 的 `subjectStatusCards`：已报名但未在读（含退费、结课等）统一走 `ssc-inactive` 灰色卡片；仅 `在读/调班` 使用学科彩色。
  - `uv-components.css`：新增 `.ssc-inactive` 灰色样式。

## 21:20 2.0 校准管理完整复刻 1.0
- 用户要求：2.0 校准管理完全复刻 1.0 功能（变动提示看板 + 导出报告 + 校准台帐 + 运行监控），UI 统一 2.0 风格。
- 实现：
  - `engine/calibrator.py`：从 1.0 `uv-dashboard/calibrator.py`（1197 行，自包含，仅依赖 stdlib+openpyxl）原样复制。其 `calibrate()` / `generate_calibrated_ledger()` / `generate_calibration_report()` 输出与 2.0 `repository.save_calibration_run` 的扁平字段（{学科}_class_id 等）完全兼容。
  - `api/__init__.py` 的 `api_calibrate`：替换 stub，复制 1.0 编排（保存上传→calibrate→生成台帐+报告→save_calibration_run→返回 stats/student_changes/change_details/anomalies/files）。新增 `import traceback / from datetime import datetime`、`from engine.calibrator import ...`、`get_data_dir`。
  - `repository/__init__.py`：新增 `get_data_dir()` 访问器（上传/输出落盘到 data_dir 下的 calibration_uploads / calibration_output）。
  - `static/js/uv-app.js`：重写 `handleCalibrate`（6 张统计卡 + 变动类型 pills + 新增/退费/续费/调班 四类学员区块 + 异常盒 + 下载按钮），移植 `formatStudentChanges` 逻辑；增强 `loadMonitor`（文件列 + 详情链接）；新增 `viewRunDetail`/`closeRunDetail`/`escHtml`。
  - `static/css/uv-components.css`：新增 `.cal-dash/.cal-stat-grid/.cal-change-card/.cal-anomaly/.tag` 等 2.0 风格类。
- 验证：node --check 通过；temp dir 全链路跑通（433 学员 / 86 变动 / 5 表报告）；HTTP /api/calibrate 返回 success，下载台帐(517KB)与报告(14KB)均 HTTP 200，报告含 校准概览/学员变动总览/变动明细/新增退费明细/重点关注 五表；运行监控 70 条记录可见。
- 注：2.0 开发模式共享 1.0 的 data_dir（../uv-dashboard），校准运行在两系统监控中均可见。
- 验证：py_compile repository/__init__.py 通过；node --check 通过；重启 5200 服务，首页返回 200。

## 校准概览配色升级 + 运行监控变动概览（#364~#366 延续）

- 用户要求：校准概览(报告 Sheet1) 标底色、新增/退费/调班饱和度更高、整体高级直观；运行监控每次校准详情展示变动概览。
- `engine/calibrator.py` `generate_calibration_report`：
  - 顶部新增高饱和配色（`green_fill #C6E7CE`/`red_fill #FAC9C9`/`orange_fill #FBE0BD`/`blue_fill #CFE0F7`/`teal_fill #C9ECE6`/`brand_fill #4F6BED`）+ `cat_fill(label)` 按 新增学员/退费/调班调期/续秋/报科 归类；新增 `Border`/`Side` 细边框；Sheet1 去掉网格线(`showGridLines=False`)、品牌标题带、核心指标(KPI)色值单元格、变动类型统计行全色、退费比例红底、重点关注色块、底部图例。
  - Sheet2 学员变动总览行底色改用 `cat_fill`（更高饱和）。
- `static/js/uv-app.js` `viewRunDetail`：解析 `run.stats_json`，顶部加「变动概览」——6 张 KPI 卡(学员/在读/新增/新增退费学员/新增退费单科/变动，按色) + 变动类型统计(彩色 tag + 计数) + 下方变动明细表；新增 `changeTagClass(label)` 映射（绿/红/橙/蓝/青/灰）。
- `static/css/uv-components.css`：新增 `.tag-teal/.tag-grey` 与 `.run-ov*/.run-ov-kpi*` 概览样式。
- 验证：HTTP 重新校准(20260711_221213)成功，下载报告 HTTP 200、网格线关闭、标题品牌色、调班行橙底确认；run detail API 返回 `stats_json.change_breakdown` 供概览使用。

## 变动详情（学员维度）视觉升级 + 运行监控统一展示（#364~#366 延续）

- 用户要求：变动详情展示截图中的「学员维度」页面；迭代更清晰，增加对应色块底色，高级美观，数据直观，文字尽量不换行。
- `static/css/uv-components.css`：
  - 校准学员卡片 `.cal-change-card` 改为整体色块背景：hd-new `#F0FDF4` 绿、hd-refund `#FEF2F2` 红、hd-fall `#F0FDF9` 蓝、hd-info `#FFF7ED` 橙；配套同色系边框、悬浮上浮阴影。
  - 卡片网格改为 `repeat(auto-fill, minmax(320px, 1fr))`，增大间距。
  - 卡片头 `.cal-cc-head` 与学科行 `.cal-ch-line` 全部 `white-space:nowrap` + `overflow:hidden` + `text-overflow:ellipsis`，避免换行；学科名加白底小圆角标签，变更描述过长时自动省略。
  - 新增 `.cal-cat-title` 分类标题（色点 + 人数），更干净。
- `static/js/uv-app.js`：
  - 抽出 `renderStudentChangeOverview(studentChanges, opts)` 复用组件，用于「校准结果」和「运行监控-详情」。
  - `renderCalibrateDashboard` 直接调用该组件渲染学员维度概览。
  - `viewRunDetail` 将原「字段级变动明细表」替换为「学员维度变动详情」；从 `run.student_changes_json` 解析数据渲染。
- `repository/__init__.py`：
  - 数据表 `calibration_runs` 新增 `student_changes_json TEXT` 列，并在 `init_db` 迁移逻辑中补齐旧库。
  - `save_calibration_run` 保存 `result['student_changes']` 供运行监控详情使用。
- 验证：node --check 通过；py_compile 通过；新校准（20260711_222217）成功，run detail API 返回 `student_changes_json` 含 13 条学员变动；运行监控详情弹窗现在渲染与校准结果页一致的学员维度色块卡片。

## 22:30 — 校准管理页风格与字段增强
- 用户反馈：将运行监控详情页的「校准变动概览」彩色 KPI 卡片风格复用到校准管理主页面；新增报名/新增退费卡片补充期次和老师。
- `engine/calibrator.py`：
  - 新增学员的 `student_changes` 详情补充学科期次/主讲老师；
  - 新增退费明细条目增加 `period`/`teacher` 字段。
- `static/css/uv-components.css`：
  - 校准管理页 `.cal-stat-card` 改为彩色背景卡片（在读=绿、新增学员=橙、退费=红、续费=青、变动=黄、总数=品牌蓝），与运行监控详情页一致。
- `static/js/uv-app.js`：
  - `formatStudentChanges` 为新增报科/新增退费每行展示「期次 · 老师 · 班号」，并保持在单行不折行。
- 验证：HTTP 校准（20260711_223001）成功，新增学员与新增退费详情均返回期次/老师字段；运行监控 DB 中 `student_changes_json` 同步保存。

## 22:42 — 调班小字信息风格对齐新增/退费
- 用户反馈：调班卡片的学科明细小字，保持与新增报科/新增退费一致（去掉冒号、括号，改用「期次 · 主讲」格式）。
- `static/js/uv-app.js`：
  - `formatStudentChanges` 的调班分支改为：`学科 调班 期次 X→Y · 主讲 旧→新`；无明细时仅显示 `学科 调班`。
  - 移除中文冒号 `：`、圆括号 `（）`，与新增/退费风格统一。
- 验证：node --check 通过。

## 22:48 — 实现两个 P0 功能缺口（数据导出 + 校准回滚）

《UV2.0体验审视报告》指出的两个 P0：①数据导出全面缺失 ②校准无回滚（运行监控只能看详情不能设为当前基线）。

### A. 数据导出 Excel（students / matrix / schedule 三类）
- 新增 `uv-dashboard-2.0/exporter.py`：`export_excel(export_type, run_id, filters=None, filter_spec=None, schedule_params=None)` → (BytesIO, 文件名)。
  - 复用现有 repository 查询（`get_filtered_students` / `get_period_point_matrix` / `get_class_schedule`），无需重写数据逻辑。
  - openpyxl 写表头品牌色 + 数据边框 + 冻结首行 + 自动筛选 + 合计行（矩阵含教学点合计/期次合计/总计，UV 用去重值；排班含学员名单列）。
- 后端 `api/__init__.py`：`/api/overview/export` (POST) 实现（原 TODO 占位），按 export_type 走 FilterSpec 或 schedule_params；`download_name` 中文文件名（前端取 filename* UTF-8）。
- 前端：`static/js/uv-export.js` 新增 `UVExport`（exportStudents/exportMatrix/exportSchedule 调 `UVFilters.buildParams`/`buildScheduleParams`）+ 自带 `UVToast` 轻量提示。`uv-api.js` 加 `exportExcel`/`pinRun`。`index.html` 在期次矩阵/排班看板/学员检索三处筛选栏加「导出」按钮；`uv-config.js` 加 `pinRun` 端点。

### B. 校准 run 回滚 / 设为当前基线（pin 覆盖层方案）
- `repository/__init__.py`：新增 `app_meta` 表；`get_current_run_id()`（pin 优先、无 pin 回退最新）、`set_current_run_id`、`clear_current_run_id`、`get_pinned_run_id`；**`get_latest_run()` 改为优先返回 pin 的 run**（全局"当前"唯一入口）；`save_calibration_run` 成功后自动清除 pin（新校准即最新）。
- 后端：`/api/runs/pin` (POST) 接收 `{run_id}`，空/"latest"=清除 pin 回最新，否则校验存在后 pin；`/api/runs` 注入 `is_current`/`is_pinned`；`/api/overview/stats` 返回 `current_run_id`/`is_pinned`。
- 前端：`loadMonitor` 表格加「当前」列 + 行高亮 + 「设为当前基线/返回最新」操作；`viewRunDetail` 弹窗底栏加同款按钮；`loadDashboard` 在 header 显示「📌 已回溯至历史基线」徽标。所有看板经 `_get_run_id()` 自动随 pin 切换。

### 验证（服务重启后 curl + openpyxl 回读）
- 三类导出均返回合法 xlsx（学员明细 1300 行/16 列、期次矩阵 21 行、排班看板 55 行）；表头与数据正确。
- pin 旧 run → /api/runs 该 run `current=true,pinned=true`、/api/overview/stats `is_pinned=true` 且 run_time 变为旧 run；pin "latest" → 回到最新 `is_pinned=false`。
- node --check 与 py_compile 全部通过。
