# 2026-07-14 字段标签字典校准

## 输入
- 读取 `Desktop/数据标签说明.xlsx`：源系统 94 列字段字典（表头+释义两行，余空）。
- 三方对照：源文件 / loader FIELD_COLS / 2.0 库实际落库值。

## 与库对照结论
- loader `FIELD_COLS` 已映射字典绝大多数重要字段；enrich_json 含 advisor_roles/channel_lead_l1-2/channel_deal_l1-2/entry_type/renew_next/renew_next_type/retain_last/renew_denom_incl/next/next2/school_grade/school_name/refund_reason* 等。
- 学科：DB 存 `雪球思维/双语素养/悦读创作/雪球科学`，`SUBJECT_LABEL` 已映射 数学/英语/语文/物理（与字典一致 ✓）。
- 班型：DB 存 `源能/启能/潜能/超能`（当前源数据无启航）；`CLASS_TYPE_ORDER` 已含启航班。
- 在班状态：行内=`在读/出班`（匹配字典）；学员级=`在读/有效退费`（派生自 valid_refund）。
- `course_type`/`product_type` 已在 subjects_json 行内（特惠/系统、转化/正价），但 enrich 为空、未做筛选项 → 缺口。
- `channel`(student_snapshots)=订单线索二级(AM)；商机二级(AO) 有值（转介绍/地推/商务地推…）未用。
- `entry_type`(入班类型) 实际=`标内PV/尾单PV`（报名时点），与字典"老生续报/纯新生/扩科目"释义冲突。
- 续报分母 `renew_denom_next`=1648 / `renew_denom_incl`=1653 有值；U2规则(CG)未启用。
- `retain_last`(是否上学季留存) 否1326/是1123；`renew_next_type` 非空 5 类（含"系统课续系统课"笔误变体）。

## 对字典的异议（已向用户提出）
1. 入班类型(列72)释义错：实际=标内PV/尾单PV（报名时点），非"老生续报/扩科目"。
2. 成单归属人(列35)释义"姓名编码，唯一"应为"姓名，未必唯一"；"唯一"属列34 uname。
3. 班级结束日期(列23)释义"改时间"→"该时间"笔误。
4. 续下一学季类型(列47)实际 5 类非空（含 系统课续系统课/特惠课续系统课 笔误），非字典所说 3 类+历史。
5. 是否联报(列74)源文件为空，且联报下下一学季(列48)释义描述"联报"本身，与74重叠 → 重复对疑为48/74而非49。

## 待用户拍板（8问）
Q1 渠道：是否加 商机二级(AO) 并列筛选，一级仅作UI分组？
Q2 课程类型：是否补抽取并加「特惠/系统」筛选项？
Q3 入班类型：用标内/尾单做"报名时点"筛选 + 另派生"新生/老生续报/扩科目"？
Q4 续报分母：默认用 不含联报(BX) 还是 含联报(BW)？是否启用U2规则？
Q5 年级双轴：是否同时保留 学校年级(CJ)+公立校 作为可选筛选？
Q6 顾问：按 uname(列34) 聚合、显示用 name(列35)，认可？
Q7 状态：出班是否细分 退费/结课，或保持 在读/出班+退费独立维度？
Q8 显示命名：学科显 数学/英语/语文/物理、班型显 源能班… 认可？

→ 用户确认后落地 `uv-dashboard-2.0/字段标签定义.md` 并接入筛选器释意+联动。任务 #444。

## #451 combos faceted 联动（前端消费）已落地
- 后端 `get_filter_options` 返回 `combos`(1136去重元组) + `combo_fields`(12字段，含 course_type/product_type/enrollment_status)。
- `static/js/uv-filters.js`：
  - init 缓存 `this.combos`/`this.comboFields`；新增 `_recomputeFacets(section)`（标准 facet：某维度可用值=满足其它已选维度的 combos 取值集）、`_pruneInvalidSelections(section)`（迭代剔除失效已选）、`_optionsForId(id)`（faceted 优先，全局回落）、`_dropdownDefs`、`renderSectionDropdowns(section)`。
  - `renderAllDropdowns` 改为逐板块委托 `renderSectionDropdowns`；`onMultiSelectChange` 改 `opts=this._optionsForId(id)`（修复 faceted 子集下"全选减一"语义错乱），选中后本板块重算+剔除+重渲染；`setGlobalBranch`/`resetSection`/`_syncGlobalBranchUI` 同步接入。
  - 覆盖 dashboard/matrix/teacherBoard/advisorBoard/student 五板块；schedule 维持 scheduleOptions 回落。
- 验证：node --check 通过；用真实 combos 单测确认空选=全集、course_type=特惠课 老师 54→43、+subject=雪球思维 23、enrollment_status=退费 正确收窄分校、失效已选被正确剔除。
- 仅改 JS，浏览器硬刷新即生效（无需 launchctl 重启）。

## #449 派生学员类型标签（新生/老生续报/老生拓科目）—— 判定为 blocked/待数据
- 核查 `student_snapshots.channel` 全为线索来源；当前 pin 为 26暑单期快照，无跨期历史/老生维度字段。
- 缺少"新生/老生"判定依据，本任务暂不可实现，待多期数据或明确历史字段口径后再做（不做筛选器，仅作派生标签放课程详细/学员卡片）。

## #454–#459 新增「全量数据上传」入口 + 合并模型（校准优先、全量兜底）
- 需求：保留现有「校准管理」(实时) 不变，新增全量订单明细上传入口（昨日数据）；差异仅时间维度，冲突以校准为准。
- 架构（物化 merged run，读取路径零改动）：
  - `calibration_runs.source` 列：'full'=全量基线(base) / 'calibration'=校准(overlay) / 'merged'=合并结果。init_db 已补列并回填历史 od_ run 为 'full'。
  - `app_meta` 新增 `base_run_id` / `overlay_run_id` 槽位（get_meta_value/set_meta_value）。
  - 全量管道 `import_to_production`：INSERT 写 source='full'，set_current 时写 base_run_id 并调 `rebuild_active_run()`（取代原直接 pin current_run_id）。
  - 校准管道 `save_calibration_run`：写 source='calibration' + overlay_run_id（不再清 pin），末尾调 `rebuild_active_run()`。
  - `rebuild_active_run()` 四分支：双空→清 pin；仅全量→pin base；仅校准→pin overlay；两者皆存→`_merge_rows` 按 uid 合并(校准主体优先，enrich 列空时由全量回填)→`_write_run` 物化 `merged_<ts>`→pin→删旧 merged（先写后删免空窗）。
  - 回退兼容：base_run_id 未设时自动取最新 source='full' run 作 base（覆盖历史仅有 od_ 全量 run 的部署）。
- API：`POST /api/upload-full`（多文件→import_to_production+重建）、`POST /api/rebuild`（手动重建）。已 import 到 api/__init__.py。
- UI：校准管理 tab 新增「全量数据上传」面板(多文件)；运行监控 tab 加「🔀 重新合并」按钮 + 运行记录「类型」徽标(badge-full/cal/merged)；JS 加 handleFullUpload/handleRebuild/onFullFilesChange/sourceBadge/refreshAll。
- 验证（DB 副本 + 生产）：`import_to_production` 写 source='full'+base_run_id+重建通过；模拟校准 overlay 后 merged run 中校准 uid 赢（退费）、全量独有 uid 保留；生产 `/api/runs` 已带 source，`/api/rebuild` 返回 base=od_20260713_period6。生产 base_run_id 已落库（显示数据不变）。
- 注意：未在生产打 `/api/upload-full`（会 pin 5 行测试 run 污染看板）；该路由经副本端到端验证 + 为薄封装，足够。
- 重启：`.py` 改动后 `launchctl kickstart -k gui/$(id -u)/com.uv.dashboard2`；前端改完浏览器硬刷新。

## 开课前续报 / 当期转化 口径校准（2026-07-14 下午）
- **问题**：看板「开课前续报PV/当期转化PV」全部归为开课前续报，因 `order_detail_loader._build_line` 把 `fall_pay_time` 置为 `None`，`classify_renewal_by_pay_time` 无支付时间时保守归为 `pre`。
- **修复**：
  1. `engine/order_detail_loader.py`：`_build_line` 将本行订单 `pay_time`（实际支付时间）写入 `fall_pay_time`。
  2. `config/__init__.py`：`classify_renewal_by_pay_time` 新增 `start_date` 参数；若提供实际班级开始日期，优先按「支付时间 < 开课日期」判定 pre/new，否则回退到期次配置表。
  3. 更新 `engine/__init__.py`、`repository/__init__.py`、`api/__init__.py`、`exporter.py` 所有调用点，传入行级 `start_date`。
- **历史数据迁移**：对 4 个 `source='full'` run（od_20260713_*）的 9796 行快照填充缺失的 `fall_pay_time`（从 `pay_time` 回填），共更新 30701 条学科行。备份：`uv_dashboard.db.bak_pre_fpt_migration_20260714`。
- **验证**：服务重启后 `/api/overview/stats` 返回 `pre_renewed_pv=3761` / `new_renewed_pv=119`，当期转化不再为 0。

## 拉新×续报转化月度图优化（2026-07-14 下午）
- **需求**：图表与上方全局筛选器联动；定位拉新学季；排除老生及老生拓科；月份归属按订单支付时间；续报用 AT 列「是否续下一学季」。
- **改动**：
  1. `engine/order_detail_loader.py`：`_build_line` 增加 `renew_next` 字段保留原生 AT 列；顺手修复 `import_to_production` 内 `datetime.datetime.now()` → `datetime.now()`。
  2. `repository/__init__.py`：重写 `get_acquisition_trends` —— 从 DB 行级 `pay_time` 取月份（不再读 Downloads Excel）；按学季顺序排除老生/拓科；行级过滤学科/期次/教学点/主讲/年级/年份/课程类型/产品类型；UV 按学员最早支付时间归月、PV 按每行支付时间归月；续报优先 `renew_next` 缺失回退 `continue_fall`；返回动态月份与次学季标签。
  3. `api/__init__.py`：`/api/overview/acquisition-trends` 增加 `year`/`course_type`/`product_type` 参数。
  4. `templates/index.html` + `static/js/uv-trends.js`：标题动态显示「XX学季拉新×续报转化 · 月度分析（排除老生及老生拓科）」；月份 chips 根据返回数据动态生成；图例/Tooltip 动态显示「未续X/已续X」；更新无数据提示。
- **数据**：重新导入广州+深圳订单明细（Desktop），生成新 run `od_20260714_152211`（2449 学员）并 pin 为基线。
- **验证**：
  - `/api/overview/acquisition-trends` 全量返回 暑假/3-7 月，排除老生 1493 人，纯拉新 762UV/1625PV，总续报率 13.0%。
  - 筛选 `branch=广州&grade=初一&period=零期` 返回 87UV/226PV，与全局 KPI 不冲突。
  - `/api/overview/stats` 广州初一零期仍为 `new=55/pre=42/renewed=97/active_pv=295`。
  - `node --check static/js/uv-trends.js` 通过，服务重启后前端可渲染。

## 拉新图验证复核（续上，curl 编码陷阱澄清）
- **澄清**：此前用裸 `curl "...?branch=广州..."`（未 URL 编码）实测返回空 + hint"未能定位拉新学季"，一度误判为回归。实际是 curl 把未编码中文传进 URL，Flask 收到乱码 → `branch` 匹配不到任何行 → 空。
- **真实验证**：用 `-G --data-urlencode` 重新实测 API，与直接调 `repository.get_acquisition_trends(set_data_dir 后)` 结果一致：
  - 无筛选 → 暑假/3-7月，old_excluded=1493，762UV/1625PV，conv 13.0%，renewed 99，next=秋季。
  - `branch=广州&grade=初一&period=零期` → 暑假/3-6月，87UV/226PV，conv 28.7%，renewed 25。
  - 前端 `UVFilters.buildParams` + `URLSearchParams(...).toString()` 已正确编码中文，生产路径无此问题。
- **结论**：5 项优化全部真实生效，无需再改代码。repository 返回值同时含 `season`/`next_season` 与 `season_label`/`next_season_label`，前端 `renderAcquisitionChart` 取 `*_label` 正确。
- **遗留小隐患（非阻塞）**：`repository/__init__.py` 中 `_is_prior_season`/`_next_season`/`_split_filter`/`_parse_pay_time`/`_resolve_target_season`/`_season_index` 存在**重复定义**（约 2051-2121 为首批，2129-2202 为重写覆盖版）。首批为死代码，建议后续清理避免两边不同步。

## 拉新图 969 vs 943 差异根因（用户手筛核对）
- **用户手筛**广州 订单明细(122429)：学季=暑(G列)、班级年级=初一(P列)、在班状态=在读(AQ列)、订单来源=拉新(BS列=`订单来源[新绩效类型](从暑开始算，仅对特惠和系统课判断)`) → **969 PV**。
- **看板** `get_acquisition_trends(branch=广州,grade=初一)` → 暑假/3-7月，**total_active_pv=943**（=用户看到的 943）。
- **根因（已端到端验证，两种口径完全对上）**：
  - 源 `订单来源=拉新` 是**按订单**的标签（含"老生拓科"误标）；看板"纯拉新"按**学员跨学季历史**排除（学员在寒/春有在读即判老生，整季剔除）。
  - Excel 969 行中，**26 行属于"寒/春有在读"的老生uid**（被源错标为拉新）→ 看板剔除 → 943。
  - 反向验证：看板保留的 943 行 **order_origin 全为 拉新**（无反向多算）；看板剔除的 143 老生uid行 = 拉新26 + 续报扩科57 + 老生60。
  - **结论**：差异=26 完全由"老生拓科被源标为拉新"造成，看板行为符合用户点#3"排除老生及老生拓科"要求，非bug。
- **注意**：DB 行内 `order_origin` 字段对这批行为 None（loader 未真正落库该列值，或仅特惠/系统课有值），看板"纯拉新"完全靠 prior-season 排除派生，未消费源的订单来源列。若未来想让看板直接对齐源的"拉新"标签，需先修 loader 落 `order_origin`，再改 `get_acquisition_trends` 用该列。

## 拉新图口径改为 B 方案：以源表「订单来源」标签为准（2026-07-14 晚）
- **背景**：用户判断春暑秋联报场景下源表 BS 列（订单来源=拉新）比看板 prior-season 排除更准，选 B 方案以 BS 列为准。
- **前期对比（仅看上一学季）**：用户曾问若只判"春季在读"是否老生，实测广州·暑·初一·在读：prior-season(寒或春)剔除→943；仅春季剔除→957（差14=寒读过但春流失的回流客）。确认"只看上一学季"与"看全部 prior"口径差14 PV，仅影响春/暑两季（冬/秋两季两口径一致）。
- **落地改动**：
  1. `_build_line` 新增 `order_origin` 行级字段（之前仅落学员级列、line 级为 None）。
  2. 重新导入广州(122429)+深圳(151736)两表 → run `od_20260714_163836`（2449 学员，全量 pin，无 overlay）。当前 run line order_origin 分布：老生4526/拉新3860/续报扩科516，0 缺失。
  3. `get_acquisition_trends`：移除 old_uids prior-season 排除；matched_lines 增加 `order_origin=='拉新'`；返回 `non_laxin_excluded`（替换 `old_excluded`）。
  4. 前端 `uv-trends.js`：标题改「（按订单来源=拉新）」、空数据提示更新；`node --check` 通过。
- **验证**：`branch=广州&grade=初一` → PV=969/UV=351/续报39/conv11.1%（与手筛 969 精确对齐）；全量 PV=1744/UV=850；全局 KPI 广州初一零期 new=55/pre=42/renewed=97/active_pv=295 不变。服务已 `launchctl kickstart` 重启。
- **注**：历史 `student_snapshots` 表累积多 run 行；`get_acquisition_trends` 严格按 run_id 过滤，旧 run 残留行不影响（直接查表须带 run_id）。

## 期次矩阵 vs 数据看板 对不上（用户指广州·暑假·初一·零期 应为 295，矩阵显示 240）
- **根因（已定位）**：矩阵 bug 不在口径逻辑，而在**筛选层级下沉**——旧 `_filter_matrix_students` 在**学员级**按 season/grade 过滤，会误删「学员级 season/grade 字段为空、但含有效 暑假/初一 行」的学员 → 其短学期行整批不进矩阵，零期列少计 55（240→295）。`get_period_point_matrix` 主循环已把 season/grade 下沉到**行级**（lines 962-971，`line_season=sd.season or student_season`，fallback 学员级）+ `_filter_matrix_students` 不再学员级筛 season/grade（line 1080 注释），恢复这 55 行。
- **关键事实**：`engine.compute(广州,暑假,初一)` 的 `by_period.active_pv` 本就=295（看板正确），修复后矩阵 `__col_pv__['零期']` 也=295，**全部期次对齐**（零期295/一期162/二期249/三期380/四期0）。
- **真凶是 stale 服务**：磁盘代码早已修好，但 launchd 服务 `com.uv.dashboard2` 仍跑旧进程（旧矩阵无 `__col_pv__` 键、零期 col_uv=93）。`launchctl kickstart -k gui/$(id -u)/com.uv.dashboard2` 重启后即生效：live `/api/overview/matrix?branch=广州&season=暑假&grade=初一` 零期=295、col_uv=117，与 `/api/overview/stats` 完全一致。
- **整体(total)矩阵≠看板属预期**：矩阵只有 零期/一期/二期/三期/四期 列（暑/寒短学期期次），看板 `active_pv=7930` 含 秋/春 整学期(period=无)无列 → 矩阵 `__grand_pv__=5051` 仅覆盖 暑+寒 短学期行（engine 暑+寒=5052，差1为某短学期行 season 非暑/寒的脏数据，非回归）。
- **经验**：改 `.py` 后必须 `launchctl kickstart` 重启，浏览器硬刷新不生效（Flask 后台进程不热重载）。验证用 `set_data_dir(项目根)` 后直接 import `repository`/`engine` 跑脚本，比 curl 编码坑更稳。

## 期次矩阵大迭代：学季感知（短学期期次 × 长学期星期，2026-07-14 落地）
- **需求（5点）**：①寒暑排版不适配全部学季 → 独立学季标记（如"暑"徽标），空教学点/空期次不进矩阵；②春秋为长学期 → 改（教学点 × 周五/周六/周日）矩阵；③校准期次须明确"暑一期/寒一期"；④寒暑矩阵上方期次时间**各分校不同**，从订单析出真实开课/结课日（同期次多数班日期相同），不用固定标签；⑤春秋矩阵日期同理。总目标：加长学期矩阵后所有期次数据可展示，风格统一。
- **后端 `get_period_point_matrix(run_id, filter_spec)` 全重写**（`repository/__init__.py` ~855-1148）。新返回 `{matrix_type, season_label, season_badge, columns, matrix, points}`。
  - 学季常量：`SHORT_SEASONS={'暑假','寒假'}`（期次维度 零/一/二/三/四期）、`LONG_SEASONS={'春季','秋季'}`（期次='无'，按 start_date 派生 weekday 维度）、`SEASON_BADGE={暑,寒,春,秋}`、`WEEKDAY_NAME/WEEKDAY_ORDER`。
  - **target_season 自动侦测**：filter_spec.season 单选则用之，否则取活跃行中最常见 season；matrix_type 由 season 归属短/长决定。
  - 短学期列 = plist 去 ('','无')；长学期列 = 各行 `datetime.fromisoformat(start_date or class_start).weekday()` → 周X。只统计 `line_season==target_season` 行。
  - **动态日期（关键）**：`col_meta[col]['pairs']=Counter((start_date,end_date))`，列日期取 **众数(most_common) 的 (start,end) 对**（非 min/max，避免脏数据跨期次拉宽区间，如三期曾误显 6/29）。格式 `M月D日-N月D日`，跨年加 `YYYY年`。
  - **空过滤**：只保留 active>0 的教学点(active_pts)与列(active_cols)。
  - 列排序：短学期 by `(start_date, _period_rank)`（`{零期:0,一期:1,二期:2,三期:3,四期:4}`，日期为主排位为辅，修 零期/一期 错序）；长学期 by WEEKDAY_ORDER。
  - 汇总键 `__row_uv__/__col_uv__/__grand_uv__` 及 pv/cls/subj_cls 系列均按 col_keys（不再 PERIOD_ORDER）。
- **前端 `static/js/uv-matrix.js` v5 全重写**：`_renderTable` 解构新形状；`colKeys=columns.map(c=>c.key)`；渲染 `.matrix-season-header`（`.season-badge.{summer|winter|spring|autumn}` 圆形 36px 渐变 + `.matrix-title`）；标题短学期"X期次 × 教学点矩阵"/长学期"X学期 × 教学点矩阵"；表头用 `c.date_range`+`c.label`；图例含雪球科学(`.dot.science`)。
- **CSS `uv-matrix.css`**：新增 `.matrix-season-header`(flex)、`.season-badge`(圆 36px，暑橙/寒蓝/春绿/秋琥珀渐变)、`.matrix-title`(16px 粗)。
- **`templates/index.html`**：面板标题 "寒暑期次 × 教学点矩阵" → "学期矩阵"。
- **`exporter.py` `_build_matrix_wb`**：读 matrix_type/season_label/columns，col_keys=[c.key]，维度名 短='期次'/长='星期'，sheet 名 `{season}矩阵`；短长学期导出均通过。
- **API `/api/overview/matrix`**：GET + query 参数（**非 POST**），FilterSpec.from_params 已含 season/branch/grade 等；返回 jsonify(matrix) 新形状零改动。
- **端到端验证（live 服务，重启后）**：
  - 广州暑假初一：short_term/暑，列[零期 6月29日-7月10日,一期 7月13日-7月24日,二期 7月27日-8月7日,三期 8月10日-8月21日]，col_pv={零295,一162,二249,三380}，与 engine 对齐。
  - 广州春季：long_term/春，列[周四 2月26日-6月28日,周六 3月14日-6月27日,周日 3月15日-6月28日]。
  - 广州秋季：long_term/秋，跨年列[周五 2026年9月4日-2027年1月2日 等]，跨年格式正确。
  - 深圳寒假：short_term/寒，列[零期,一期,二期]。
  - 自动侦测(无 season)→ short_term/暑假(最常见活跃学季)；period 单选→仅该列；空(五期)→ cols=[]/points=[]/grand_pv=0。
- **遗留**：浏览器视觉未在本环境亲验，需用户硬刷新确认徽标/星期列样式。

## 修正：春季矩阵列必须来自 X 列（week_cycle/上课周期），而非开课日（2026-07-14 修复）
- **问题**：用户截图显示广州春季矩阵出现「周二、周四」，但底表 X 列（上课周期）只有 周五/周六/周日。总 PV 612 对得上，但列来源错误。
- **根因**：长学期矩阵此前从 `start_date/class_start` 推导星期。某些行的开课日（如 5月19日 周二、2月26日 周四）与 X 列 `week_cycle` 不符，导致额外列。
- **修复**：`repository/__init__.py` `get_period_point_matrix` 长学期列键直接读 `sd.get('week_cycle')`；支持 `每周五/每周六` 或 `周五/周六` 形式；规范化 `每周五 → 周五`；空值则跳过。
- **验证**：
  - 广州春季：列仅 周五/周六/周日，grand_pv=612，与截图一致；周二/周四消失。
  - 广州暑假初一：短学期未受影响，零期=295/一期=162/二期=249/三期=380。
  - 广州秋季：列 周四/周五/周六/周日（由 X 列实际值决定）；深圳寒假：零期/一期/二期正常。

## 学期矩阵筛选器三处改进（2026-07-14 晚，Phase C）
用户针对矩阵筛选栏提 3 点：①选一条件后其它唯一维度自动显示该唯一值（非"全部"）；②产品类型改为 班课/1v1（取 M 列 课程模式），默认 班课；③加 学科 筛选器。
- **①联动自动选中（仅矩阵板块）**：`uv-filters.js` 新增 `_autoSelectUnique(section)` + `_fieldToMsId` + `_autoSelected` 集合。逻辑：某维度 faceted 可用项恰好 1 个 → 自动选中该值（显示而非"全部"）；该维度不再唯一（如取消分校使 year 变多值）→ 回退 全选；用户手动改过的维度打 `delete(_autoSelected)` 不被联动强制锁。在 `onMultiSelectChange`(仅 matrix)、`setGlobalBranch`、`resetSection`(matrix)、`init` 四处调用。Node 模拟验证：选广州→year 自动=2026；取消分校→year 回退空（✓）。
- **②产品类型→班课/1v1（class_mode）**：原 `product_type`(J列 正价品/转化品) 仅改矩阵维度，新增独立字段 `class_mode`，归一 `k9班课→班课`/`k9一对一→1v1`：
  - `config/__init__.py`：加 `norm_class_mode(v)` + `FILTER_DEFAULTS['class_mode']='班课'`。
  - `engine/__init__.py` FilterSpec 加 `class_mode` 字段/from_params 参数；compute 行级过滤加 class_mode。
  - `repository/__init__.py`：`get_filter_options` 收集 `class_mode`→返回 `class_modes=['1v1','班课']`，并加入 `COMBO_FIELDS`+combos 元组；矩阵/通用/拉新(trends+acquisition) 四处过滤加 class_mode。
  - `api/__init__.py`：matrix 与 acquisition-trends 端点透传 `class_mode`。
  - 前端：矩阵 `ms-mtx-product_type` 替换为 `ms-mtx-class_mode`（label 仍"产品类型"，选项 班课/1v1，默认 班课；dashboard 的 产品类型=正价品/转化品 不动）；`state.matrix` 用 `class_mode` 替 `product_type` 并加 `subject`；`_msOptionsMap/_msStateMap/_dropdownDefs/buildParams/_setDefaultSet/resetSection` 同步；`data-glossary` 给矩阵"产品类型"写"班课/1v1（来自课程模式M列），默认班课"以覆盖全局 product_type 释义。
- **③学科筛选器**：矩阵加 `ms-mtx-subject`（选项 SUBJECTS，默认全部），前述 state/map/defs/buildParams/defaults 同步接入；COMBO_FIELDS 本已含 subject 故 faceted 联动自动生效。
- **验证**：`py_compile` 通过；`node --check uv-filters.js` 通过；live 服务重启后 `/api/overview/filters` 返回 `class_modes=['1v1','班课']`；matrix `branch=广州&season=暑假&grade=初一&class_mode=班课` → short_term/暑 gpv=1086（4 列）；`class_mode=1v1` → 0（该子集无 1v1）；`class_mode=班课&subject=雪球思维` → 366；`branch=广州&season=春季&class_mode=1v1` → long_term gpv=1（确认 1v1 过滤真实生效）。
- **注意**：curl 实测中文参数须 `-G --data-urlencode`，否则 Flask 收乱码→空结果（前端 `URLSearchParams` 已正确编码，生产无此坑）。浏览器视觉（自动选中交互）本环境未亲验，需用户硬刷新确认。

## 排班看板班级卡片再迭代（2026-07-14 晚）
- **需求**：①春/秋长学期徽章不要分两截，一整个色块；②夏/冬字改为 暑/寒；③卡片显示上课时间（CD 列 `U2开课时间`）。
- **改动**：
  1. `static/js/uv-config.js`：`SEASON_COLORS` 的 key 由 `夏/冬` 改为 `暑/寒`；徽章长学期仍整颗渐变。
  2. `static/css/uv-components.css`：移除 `.season-夏/冬/春/秋` 的重复内阴影，改为 `.cls-period-badge:not(.long)` 才给季节段加右分隔线；`.long` 徽章无分隔线，形成完整色块。
  3. `engine/order_detail_loader.py`：`FIELD_COLS` 新增 `time_slot → 'U2开课时间'`；`_build_line` 用 `_format_time_slot` 将 datetime 字符串格式化为 `HH:MM`。
- **数据**：重新导入广州(122429)+深圳(151736)两表 → run `od_20260714_200845`（2449 学员），设为 base/当前 run；旧 run `od_20260714_163836` 保留。
- **验证**：
  - `/api/overview/classes` 返回 682 班，全部含 `time_slot`（如 `15:50`、`13:20`、`18:20`）。
  - 服务 `launchctl load` 重启后正常响应。
  - `node --check uv-config.js` 与 `py_compile engine/order_detail_loader.py` 通过。
- **注意**：浏览器需硬刷新（Cmd+Shift+R）以加载新 JS/CSS。

## 排班看板班级卡片再迭代-2（2026-07-14 晚）
- **需求**：①春秋等长学期徽章单字标签居中；②班级按「年份+分校+学季」范围唯一展示 class_id，且每班学员按 UID 唯一计数；class_id 取自 T 列(班级ID)。
- **诊断结论**：DB/class_id 本就干净——`FIELD_COLS['class_id']='班级ID'`(T列)已正确；752 个 class_id 零跨季/跨属性冲突；年级/分校/学季过滤 API 均生效(year=2025→0 班)。唯一真实 bug：5 个班因同一学员多订单行被重复计入（各自多 1 人）。
- **改动**：
  1. `static/css/uv-components.css`：`.cls-period-badge` 加 `justify-content:center`；`.cpb-season/.cpb-period` 加 `text-align:center` 且 `letter-spacing:0`（消除单字因 letter-spacing 产生的轻微右偏）。
  2. `repository/__init__.py` `get_class_schedule`：第二遍构建新增 `seen_uids[cid]` 集合，同一 uid 在同一班只计一次（UID 唯一）。grouping 仍以 class_id 为唯一键（不纳入 year/branch/season，避免 year 空值把同班拆成多卡）。
- **验证**：`py_compile` 通过；重启后 `/api/overview/classes` 682 班、0 重复 class_id、0 个班 total≠唯一uid；5 个原重复班修正(5230727:23→22 等)；广州+2026+暑假=113 班。

## 学员详情弹窗"班级ID对不上"诊断与修复（2026-07-14 晚）
- **用户质疑**：排班看板两张卡片 4632662(暑一期 15:00 N教室4) 与 4632661(暑一期 18:00 N教室1) 学员重叠，且点学员弹窗显示的班级ID与卡片对不上。
- **诊断结论**：排班看板本身无错乱——两班都是源表 T 列(班级ID) 真实存在的独立班级，17 人各自正确、零重复 class_id；重叠是同一批学员同时报了 15:00 与 18:00 两个时段（同 刘明娇/雪球思维，合法双报）。"班级ID对不上"是**学员详情弹窗的显示 bug**：`get_student_detail_enhanced` 用 `display_subjects` 把同一学员的多笔雪球思维订单行折叠成 1 行（仅留 1 个 class_id），点卡片里的学员只显示了某个任意 class_id，造成"对不上"的错觉。
- **修复**：
  1. `repository/__init__.py` `get_student_detail_enhanced`：新增 `student['subjects_raw']`（按真实学科归组的未折叠行列表，含每行的 class_id/period/season/time_slot/room/renewal_class），并对 folded + raw 两套行统一做续报 enrichment。
  2. `static/js/uv-modal.js` `_renderStudentDetail`：渲染 `subjects_raw[subj]` 为多条 `.sc-class-row`（班序/class_id/主讲/期次徽章/班型/时间/教室/续报），标题显示"共 N 个班"。
  3. `static/css/uv-components.css`：新增 `.sc-class-*` 系列样式（行列表/班序/班号/主讲/期次/班型/时间/教室/续报色标）。
- **验证**：`py_compile` + `node --check` 通过；重启后 `/api/overview/student/4817354817`(高敬一) 返回 `subjects_raw.雪球思维` 4 行（4632662/4512463/3252194/4632661，各带正确 class_id/时间/教室/续报）；`/api/overview/class/4632662` 与 `/4632661` 各 17 人。浏览器需硬刷新（Cmd+Shift+R）。
- **注意**：若用户硬刷新后仍见"班级ID错乱"，需其提供截图定位（当前数据/逻辑已无重复或合并）。

## 学员详情弹窗"跨学季混排错乱"修复（2026-07-14 晚，接上）
- **用户反馈**：上一步返回全部学季行导致"学员显示错乱"——同一学员(如高敬一)从寒假跟到秋季会分处不同学季班级，弹窗却把 4 个学季的班全列出来。要求：班级展示先确认 年份/分校/学季(class_id 去重=该学季所有班)；学员归属判定须对应当前筛选学季的同期在读，不同学季下学员详情所展示班级应不同。
- **根因**：`get_student_detail_enhanced` 无学季过滤，一次性吐出 `subjects_raw` 全部学季行；前端 `openStudentDetail` 不传学季。
- **修复**：
  1. `repository/__init__.py`：
     - `get_student_detail_enhanced(run_id, uid, season='')` 新增 season 参数：传入时先把 `raw_subjects` 过滤为 `(line.season==season) and ('在读' in status)`，再 `display_subjects` + 重建 `subjects_raw`；返回 `scope_season=season`。
     - `get_class_detail` 捕获首个匹配行的 `target_season` 并在返回 dict 加 `'season': target_season`。
  2. `api/__init__.py` `/api/overview/student/<uid>` 读 `?season=` 透传。
  3. `static/js/uv-api.js` `getStudentDetail(uid, season='')` 拼 `?season=`。
  4. `static/js/uv-filters.js` 新增 `singleSeason(section)` + `currentSeason()`（按 student/schedule/dashboard/advisorBoard/teacherBoard 顺序取首个单学季，多学季→null）。
  5. `static/js/uv-modal.js` `openStudentDetail(uid, season)`：显式 season 优先，否则 `UVFilters.currentSeason()`；班级详情学员行 `onclick` 传 `data.season`；弹窗顶部加「学季作用域：X｜仅显示该学季在读班级」徽标。
  6. `static/js/uv-app.js` 排班卡片学员点击 `UVModal.openStudentDetail(uid, c.season)`。
  7. `static/css/uv-components.css` 加 `.sd-scope-row/.sd-scope-pill/.sd-scope-hint`。
- **验证**（重启后 curl，uid=4817354817/高敬一）：
  - `?season=暑假` → 雪球思维[4632662,4632661]、双语素养[5170258]，全 暑假在读 ✓
  - `?season=秋季` → 雪球思维[4512463]、双语素养[5170227]，全 秋季在读 ✓
  - 无 season → 全部 4 学季（向后兼容）✓

## 顶部全局筛选栏：年份/分校/学季/课程类型/产品类型（2026-07-14 深夜，大迭代）
- **需求**：将 年份/分校/学季/课程类型/产品类型 五个筛选器固定为顶部全局栏（Header 与 Main Tabs 之间），联动所有页面；各页局部副本全部移除；风格与 2.0 浅色 UI 统一。
- **关键架构决策（重要）**：`产品类型` 在数据层有两个不同字段——
  - `product_type`(J列) = 转化品/正价品（dashboard/teacherBoard/advisorBoard/student 原"产品类型"下拉用此）；
  - `class_mode`(M列) = 班课/1v1（matrix/schedule 的"产品类型"下拉实为 class_mode，历史误标）。
  - 为让**单一全局 产品类型 联动所有页面**，全局栏统一映射到 `class_mode`（班课/1v1）。即全局"产品类型"= 班课/1v1，下拉选项 `UVFilters.options.class_modes`，sync 到各板块 `class_mode` Set。dashboard 原 转化品/正价品 维度改由全局 班课/1v1 承接（不再单独可筛）。
- **`uv-filters.js`（Task #518 已完成，本步收尾）**：
  - `state.global={year,branch,season,course_type,product_type}`；`GLOBAL_FIELDS`、`_fieldForSection`(product_type→class_mode)、`globalSingle()`、`_syncGlobalToSections()`、`_onGlobalChange()`(sync→faceted→prune→autoSelect(matrix)→branchScoped→renderGlobal+renderAll→_refreshCurrentView→分享卡刷新)、`resetGlobalFilters()`、`renderGlobalDropdowns()`。
  - `onMultiSelectChange` 识别 `fSection==='global'` → `_onGlobalChange`。
  - **修复联动缺口**：`buildParams` 的 teacherBoard/advisorBoard/student/trends 原只 emit `product_type`，不 emit `class_mode` → 全局 产品类型 对这四块不生效。已补 `class_mode: this.options.class_modes` 到这些板块的 optionSources；trends 加 `class_mode` 到 dashFields/dashSrc；teacherBoard/advisorBoard/student 现 emit `class_mode` ✓。
  - 清理：删除旧 `_syncGlobalBranchUI`/`initGlobalBranchSwitch`/`setGlobalBranch`/`globalBranch`；`_msOptionsMap/_msStateMap/_dropdownDefs` 移除全部上收的局部下拉 id（ms-year/ms-branch/ms-season/ms-course_type/ms-product_type 及各页 mtx-/sched-/tb-/ab-/student- 对应项），仅保留各页专属维度。
- **`templates/index.html`**：① 删 `#global-branch-switch`；② Header 与 Main Tabs 间插入 `.global-filter-bar`（`ms-g-year/ms-g-branch/ms-g-season/ms-g-course_type/ms-g-product_type` + 重置按钮 `UVFilters.resetGlobalFilters()`）；③ 从 dashboard/matrix/schedule/teacherBoard/advisorBoard/student 六栏移除 年份/分校/学季/课程类型/产品类型 五项（matrix `ms-mtx-class_mode`、schedule `ms-sched-class-mode` 即产品类型）。
- **`uv-app.js`**：删 `UVFilters.initGlobalBranchSwitch()` 调用。`uv-share-card.js:80` + `uv-trends.js:717` 的 `UVFilters.globalBranch` → `UVFilters.globalSingle('branch')`。`uv-trends.js` 拉新×续报 `getAcquisitionTrends()` 补传 `UVFilters.buildParams('trends')` 使其也随全局筛选联动。
- **`api/__init__.py`**：`/api/trends/data` 的 `FilterSpec.from_params` 补 `class_mode` 参数（原只接 product_type），使主趋势图随全局 产品类型 联动；`/api/overview/acquisition-trends` 本已支持，前端补传即可。
- **CSS `uv-base.css`**：删 `.global-branch-switch/.gbs-*` 旧样式，新增 `.global-filter-bar`（视觉同 `.section-filter-bar`，sticky top:0；内部复用 `.sf-group/.sf-label/.multi-select`，紧凑样式 `.global-filter-bar .ms-text{min-width:80px;max-width:130px}`）。
- **验证**：`node --check` 四个 JS 通过；`py_compile api/__init__.py` 通过；`launchctl kickstart` 重启后 `curl /` = HTTP 200 且页面含 `global-filter-bar`/`ms-g-*`/`resetGlobalFilters`；全仓已无 `globalBranch/initGlobalBranchSwitch/_syncGlobalBranchUI` 残留引用。
- **用户须知（歧义点）**：全局"产品类型"最终取 **班课/1v1（class_mode）**，而非 转化品/正价品（product_type）。若用户本意是 转化品/正价品，需把全局栏 `ms-g-product_type` 选项改回 `product_types` 并把 `_fieldForSection` 的 product_type→class_mode 反转、各板块 buildParams 改回 emit `product_type`——但 matrix/schedule 后端仍按 class_mode 过滤，需同步改这两块后端映射，工作量更大。当前实现与"联动所有页面"最一致（matrix/schedule 本就用 class_mode）。
  - `/api/overview/class/4632662` → `season=暑假` ✓
- **结论**：三处下钻路径(排班卡→学员 / 班级详情→学员 / 当前单学季筛选→学员)均已按学季作用域收敛；多学季同选时回落全部学季（不强制）。浏览器需硬刷新（Cmd+Shift+R）加载新 JS/CSS。

## 学员详情弹窗卡片 UI 迭代（2026-07-14 深夜）
- **用户反馈**：弹窗里的班级卡片像"乱码"——文字排列错行、风格不统一、看起来像滑动/溢出。截图学员：吴梓琳/4315709405，春季在读，两科(雪球思维/双语素养)各 1 个班。
- **根因**：`.subject-cards {display:flex}` + `.subject-card {flex:1}` 把 4 个学科卡强制挤成等宽 4 列；每个已报卡只有约 25% 弹窗宽度，内部班级行再用 `flex-wrap: wrap`，导致 班号/老师/时间/教室/班型/续报 全部被迫换行，甚至出现单字竖排。
- **修复**：
  1. `static/js/uv-modal.js`：把已报科目与未报科目分开渲染——已报卡进入 `.subject-cards-active` 网格，未报卡进入 `.subject-cards-empty` 紧凑四列占位。
  2. 班级行 HTML 改为"左双行 + 右竖列"：
     - 左侧 line1：班号 + 主讲老师（不换行）
     - 左侧 line2：期次徽章 + 班型 + 续报状态（不换行）
     - 右侧：上课时间 + 教室（右对齐、不换行）
  3. `static/css/uv-components.css`：
     - 弹窗 `max-width` 780px → 900px；
     - `.subject-cards-active` 用响应式网格 `repeat(auto-fit, minmax(360px, 1fr))`，保证已报卡最小宽度足够；
     - `.subject-cards-empty` 用四列紧凑网格，降低未报卡对有效空间的挤压；
     - 全面重定义 `.sc-class-row*`：所有关键行 `white-space: nowrap`，教室做 `text-overflow: ellipsis` 兜底。
- **验证**：`node --check uv-modal.js` 通过；服务 `launchctl kickstart` 重启；curl 检查吴梓琳春季数据：雪球思维/4152035/张盘龙/L教室4/15:00，双语素养/3252161/任雯美/L教室4/18:00，字段完整无空格截断。
## 班级详情弹窗：年级 + 班级内续报标签修正（2026-07-14 晚）
- **用户反馈**：班级详情页头部缺年级；学员列表的「续报」标签与当前学季该学员在该班级的实际续报情况不一致。
- **根因**：`get_class_detail` 用 `display_subjects` 折叠后的学科行来判断续报（`subjects.get(target_subject)`），当同一学员同科目同季存在多班时，折叠行会取错 `continue_fall`/`fall_pay_time`/`period`，导致标签错位。
- **修复**：
  1. `repository/__init__.py` `get_class_detail`：
     - 头部补抓 `target_grade`（首个匹配行的 `grade`）并返回 `'grade': target_grade`。
     - 每个学员改为**直接遍历 `subjects_json` 原始行**，定位 `class_id` 完全匹配的那一行，用该行的 `status/continue_fall/fall_pay_time/period/start_date/grade` 计算续报与状态；仅在原始行缺失时以折叠行兜底。
  2. `static/js/uv-modal.js` `_renderClassDetail`：头部信息行加年级徽章 `<cd-grade-badge>`；学员状态标签对非在读/非退费（如「出班」）显示实际状态。
  3. `static/css/uv-components.css`：新增 `.cd-grade-badge` 样式（胶囊、浅灰底、不折行）。
- **验证**：`py_compile` + `node --check` 通过；服务重启；curl 查 class 3311581：年级=初一；邓雅月/萧灏轩等原先误显「开课前续报」的学员改为「当期转化」（与其 class 3311581 行的 fall_pay_time > start_date 一致）；李桐嘉/张彧瑞等「出班」学员状态正确显示为「出班」而非折叠后的「在读」。
- **使用**：浏览器硬刷新（Cmd+Shift+R）后打开班级详情弹窗即可看到年级与修正后的续报标签。

## 修复排班看板班级卡片学员标签点击报错（2026-07-14 深夜）
- **用户反馈**：点击班级卡片上的学员名字报错 `Uncaught ReferenceError: c is not defined`。
- **根因**：`static/js/uv-app.js` `renderClassCards` 里事件委托处理学员标签点击时，写了 `UVModal.openStudentDetail(uid, c.season)`，但变量 `c` 是 `classes.forEach` 循环内部变量，事件委托在循环外注册，运行时 `c` 未定义。
- **修复**：
  1. 卡片 `<div class="schedule-class-card">` 加 `data-season="${c.season || ''}"`。
  2. 点击处理改读 `card.dataset.season || activeSeason || ''`，再传给 `UVModal.openStudentDetail`。
- **验证**：`node --check uv-app.js` 通过；服务重启；curl 确认 `/api/overview/classes` 返回卡片带 `season` 字段；本地简单 JS 执行验证 `card.dataset.season` 路径可达。
- **使用**：浏览器硬刷新（Cmd+Shift+R）后，点击班级卡片上的学员名字即可正常打开学员详情弹窗。

## 跨学季续报年份倒挂修复（2026-07-14 深夜）
- **用户反馈**：秋季的班为什么有续报学员？秋季下一季应是 27 年寒假（未发生），不能接回 26 年寒假数据。
- **根因**：`engine/order_detail_loader.py` 的 `enrich_cross_season_renewal` 仅按**学季名**配对（`秋季`→`寒假`），数据里秋季2026被错误配到**同年的寒假2026**（1月早于9月，属过去冬季），导致秋季2026共1424行中有1101行被错标 cf=是。
- **数据实证**：激活 run `od_20260714_200845`（2449学员）仅含 2026 全年四学季（寒假2386/春季1836/暑假3256/秋季1424 行），无任何 2027 数据。
- **修复**：
  1. `engine/order_detail_loader.py`：新增 `_next_year_season(year, season)` = 寒假Y→春季Y→暑假Y→秋季Y→**寒假(Y+1)**；`enrich_cross_season_renewal` 索引由 `学季` 改为 `(year,season)` 元组，仅当学员在 `_next_year_season(Y,S)` 同名下有在读订单才计续报（秋季Y 无次年冬季→不计）。
  2. 写一次性脚本 `/tmp/regen_renewal.py` 导入修正函数，对全部 9 个 run 重算写回 `subjects_json`（改 6688 行）。重算前已备份 DB 至 `/tmp/uv_dashboard_backup_20260714_231741.db`。
- **验证结果**（激活 run）：
  - 修正后续报(cf=是)分布：寒假2026=1666、春季2026=1273、暑假2026=1464、**秋季2026=0**（此前1101）。
  - 陈中帅秋季班 5230705：renewed=0/pre=0/new=0/rate=0.0（此前 renewed=15）。
  - 李倬承雪球思维：暑假=是、秋季=否、寒假=是、春季=是（符合时间顺序）。
  - 暑假班仍正常续报（如 5230752：续报10，率90.9%），其它学季未受损。
  - `py_compile` 通过；服务 `launchctl kickstart` 重启。
- **注意**：以后若导入 2027 寒假数据（标注 year=2027），秋季2026 将自动正确计续报；当前因无次年冬季数据，秋季班续报显示 0 是正确行为。

## 学员详情/超能班/教学点 三项前端修复（任务 #503/#504/#505，已验证）
- #503 学员详情页学季小标统一：uv-modal.js 班级行改用 `UVConfig.seasonPeriodBadge(period, season)`（复用排班卡双段徽标），春/秋/暑/寒四季统一；uv-components.css 隐藏旧 `.sc-class-period` span 并加 `.sc-class-row .cls-period-badge` 窄行样式。
- #504 超能班穿模：`.ct-pill` 加 z-index 层级（`.ct-content`/`.ct-text` z-index:2，流光`::after` z-index:1），uv-config.js `classTypePill` 内容包 `.ct-content`；巡检其余徽标（学科 pill/教学点 chip/续报标签）无同类问题。
- #505 教学点合并为「新宝利大厦」：config/__init__.py `normalize_teaching_point` 保留"大厦"后缀（新宝利→新宝利大厦、天成→天成大厦），ROOM_POINT_MAP/ADVISOR_POINT_TAG/POINT_COLOR 全部统一；uv-config.js 顾问标签+配色 key 同步；order_detail_loader._norm_tp 委托 normalize_teaching_point；28 个 shares html 批量替换。DB 本就全名，迁移 0 行改动（备份 uv_dashboard.db.bak_20260715_103217）。
- 服务已 `launchctl kickstart -k` 重启，HTTP 200。用户待确认：是否要彻底隐藏教学点字段（当前为"统一命名"而非"隐藏"）。

## 班型标签加宽 + 续报状态按课程类型区分 + 排班看板新增筛选（任务 #506/#507/#508）
- #506 班型标签加宽：uv-components.css `.ct-pill` padding 3px12px→5px20px、min-width:72px、font 11.5→12、letter-spacing .5px；`.cls-type-pill` padding→5px16px、min-width 44→64、max-width 90→110。仅 CSS，视觉更和谐。
- #507 续报状态按课程类型(K列 course_type)区分：规则=特惠课才有转化细分（开课前续报/当期转化/已续报/未续报），系统课(及未知)一律只 已续报/未续报（忽略 pre/new）。
  - 前端：uv-modal.js 新增模块级 `_uvRenewalKind(isR, rCls, isActive, courseType)` 返回 pre/new/renewed/not-renewed/none；三处续报渲染（班级详情学员列表、学员详情三科banner、学员详情班级行）统一改用它，courseType 分别取自 `data.course_type`/`sd.course_type`/`line.course_type`。
  - 后端：repository `_renewal_label(cf,cls,course_type='')` 增加 course_type 入参（特惠课才细分，否则归已续报）；调用点传入 `sd.get('course_type','')`。`get_class_detail` 新增收集并返回 `course_type`（班级头部新增 `.cd-course-type` 蓝色标签展示）。
  - 数据佐证：系统课班 5230752 有 pre=10/new=1，前端将强制显示 已续报。
- #508 排班看板新增「课程类型(特惠课/系统课)」「产品类型(班课/1v1)」两个多选下拉：
  - 后端：`get_class_schedule` 增 `course_types=None, class_modes=None` 参数；pass1 收集 `all_course_types`/`all_class_modes`（class_mode 经 norm_class_mode 归一）；pass2 在 other_match 与完整筛选两处加 `course_type`/`class_mode` 过滤；class dict 加 `course_type`；return 增 `course_types`/`class_modes`。`/api/overview/classes` 解析并透传两参数。
  - 前端：uv-filters.js `state.schedule` 加 `course_type`/`class_mode` Set；`_msOptionsMap`/`_msStateMap`/`_dropdownDefs.schedule` 各加 ms-sched-course-type/ms-sched-class-mode；`buildScheduleParams` optionSources+fields 加二者；`resetSection` else 分支清空二者。uv-app.js `loadSchedule` 填充 `scheduleOptions.courseTypes/classModes` 并 `_prune` 二者。templates/index.html 排班筛选栏班型后加两个 .sf-group 区块。
  - 验证：系统课=494/特惠课=188/班课=675/1v1=7/系统课+1v1=7 均正确；node --check + py_compile 通过；`launchctl kickstart` 重启 HTTP 200。
