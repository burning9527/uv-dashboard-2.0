# 2026-07-10 工作日志

## UV Dashboard 2.0 — 拉新×续报转化图表迭代

### 修复：Chart.js canvas 不支持 CSS 变量导致标签模糊/颜色丢失
- **根因**: Chart.js 在 `<canvas>` 上渲染，canvas 不支持 `var(--text-tertiary)` 等 CSS 变量，导致 tick 标签、tooltip 颜色无法解析，显示模糊/透明
- **修复**: 在 `uv-trends.js` 顶部定义 `CHART_COLORS` 常量对象，用实际 hex 色值替换所有 `var(--xxx)` 引用（共11处）
- **影响范围**: 业务趋势图 `renderCharts` + 拉新×续报转化图 `renderAcquisitionChart` 两个图表均修复

### 升级：拉新×续报转化图表配色
- UV组（学员维度）: 深靛蓝 `#4263EB` + 深翠绿 `#2F9E44`（实色）
- PV组（科目维度）: 浅靛蓝 `#91A7FF` + 浅翠绿 `#69DB7C`（实色+1px深色边框）
- 转化率折线: 深橙 `#E8590C`
- 数据标签字体加大到11px+700粗细，白色（UV组）/深色（PV组）
- HTML图例同步更新为实色+边框样式

### 修正：配色回归2.0设计系统令牌
- **根因**: 上一轮用的 `#4263EB`/`#2F9E44`/`#91A7FF`/`#69DB7C`/`#E8590C` 全部不在2.0设计系统内，与上方业务趋势图的 `#378ADD`/`#16A34A`/`#E8943B` 不一致，画面不和谐
- **修正**: 全部替换为2.0设计令牌：
  - UV未续秋 → `#4F6BED` (--c-primary)
  - UV已续秋 → `#16A34A` (--c-accent-green/--c-success)
  - PV未续秋 → `rgba(79,107,237,0.35)` (primary半透明)
  - PV已续秋 → `rgba(22,163,74,0.35)` (green半透明)
  - 转化率折线 → `#E8943B` (--c-accent-orange)
- **同步对齐**: 字体大小(10px匹配趋势图)、layout padding(top:18)、tension(0.4)、lineWidth(3)、移除y轴标题（趋势图也没有）
- **教训**: Chart.js canvas 色值必须严格使用 `:root` 中定义的设计令牌对应hex值，不能随意用非系统色

### 新增：PV维度数据（上一轮完成）
- 后端 `get_acquisition_trends()` 新增 `active_pv`/`renewed_pv`/`not_renewed_pv`/`conv_rate_pv` 字段
- PV逻辑 = 科目维度（学员报2科计2 PV）
- 图表改为双柱并排：UV组(stack='uv') + PV组(stack='pv')，各柱堆叠（底部未续秋+顶部已续秋）

### 数据扩展至7月 + 校准归零（本次）

- **问题根因**: `get_acquisition_trends()` 的 `target_months` 硬编码为 `['2026-03','2026-04','2026-05','2026-06']`，漏掉7月18人 → 拉新转化只统计331，与总UV 405-老生56=349 偏差18
- **修复**: `target_months` 加入 `'2026-07'`
- **校准验证**: 总在读UV=405，老生channel='春季在读老生'=56，拉新转化=349 → 405-56-349=0 完美校准
- **前端**: index.html 月份chips 新增 7月（`data-month="07月"`）
- **数据标签位置调整**（用户要求）:
  - 拉新（未续秋, 蓝#4F6BED）: 标签**柱内下沉底部** → `anchor:'start', align:'center', offset:-6`（负offset内移），白色文字始终可见
  - 转化（已续秋, 绿#16A34A）: 标签**柱外置顶** → `anchor:'end', align:'top', offset:6`，绿色文字在白底始终可见
  - 移除旧的 `isSmallSegment` 小数据外移hack（已不需要）
  - 配色原则: 白字配彩柱 / 绿字配白底，杜绝不可见色

### 环境注意
- 沙箱Bash无法稳定直连localhost（HTTP代理52568返回502，curl --noproxy/NO_PROXY均失败）；验证后端数据请用 `python3 -c "import urllib.request,json; ..."` 直连 `http://127.0.0.1:5200`
- 2.0开发模式DB路径: `/Users/wangboning/WorkBuddy/2026-06-29-13-19-59/uv-dashboard/uv_dashboard.db`（共享1.0库），非 uv-dashboard-2.0/ 下的副本
- 启动服务器用 `run_in_background:true` 的Bash调用，否则Bash调用结束进程被沙箱回收（nohup也不行）

### 拉新×续报转化图表再次调整（本次）

- **底部标签被截断**：`layout.padding.bottom` 从 0 增加到 18，同时给纯拉新数据标签添加 `clip: false`，避免底部数字被 canvas 裁剪
- **Tooltip 精简**：移除「UV在读拉新」和「排除春季老生」两行，只保留用户要求的四项：已续秋、未续秋、纯拉新、转化率
- **蓝色柱含义改为「纯拉新总数」**：从原来的「未续秋」改为「已续秋 + 未续秋 = 纯拉新总数」，即排除春季老生后的当月拉新总量
- **移除绿色已续秋柱段**：因蓝色柱已代表总量，绿色堆叠段会造成双重解读，故去掉；转化率仍由橙色折线展示
- **图例同步精简**：只保留「纯拉新（人/科）」和「转化率 %」两项
- **数据标签位置**：纯拉新数字仍放在柱内底部（白色文字），转化率百分比仍放在折线点上方（橙色文字），确保所有颜色始终可见

### 拉新×续报转化图表再调整（本次）

- **恢复绿色已续秋堆叠段**：用户要求堆叠展示，顶端绿色=已续秋，下方蓝色=纯拉新
  - 结构上恢复 blue(未续秋) + green(已续秋) 堆叠，总高 = 纯拉新
  - **蓝色段数据标签显示「纯拉新」总数**（用 `pureNew[ctx.dataIndex]` 取总量），而非该段本身的未续秋值
  - 绿色段数据标签显示「已续秋」值，柱外置顶
  - 图例恢复为三项：纯拉新 / 已续秋 / 转化率%
- **月份筛选改为多选**：`acqMonthFilter` 从字符串改为数组 `[]`；`switchAcqMonth` 改为 toggle 逻辑（点击追加/移除，空='' 清空）；筛选时对 `month_label` 用 Set 匹配；"全部" chip 在数组为空时高亮
- **注意**: 蓝色段视觉高度=未续秋，但其标签显示纯拉新总量——这是用户明确要求的设计（底端显示纯拉新、顶端显示已续秋）

### 拉新×续报转化图表四项再调整（本次）

1. **折线置顶**: 转化率折线数据集 `order: 99`（高于柱的默认 order 0），确保折线绘制在柱状图之上、不被遮挡
2. **蓝色柱内标签恢复 + 重定义蓝色段**: 改为 blue 数据集 `data = pureNew`（柱高=纯拉新总量），绿色段 `data = renewed` 堆叠在蓝色之上（总高=纯拉新+已续秋）。蓝色数据标签 `anchor:'start'/align:'start'/offset:6` 显示纯拉新值（白字柱内底部），绿色标签柱外置顶显示已续秋值
   - 关键: 之前 blue=未续秋导致未续秋为0的月份无蓝柱、标签消失；改为 blue=纯拉新(各月均>0)后标签必然存在
3. **每月拉新总数行**: 图表下方新增 `#acq-month-totals` 容器，由 `renderAcqMonthTotals()` 渲染各月纯拉新数 + 合计卡片（CSS: `.acq-month-totals`/`.acq-total-cell`/`.acq-total-sum`）；index.html 的 canvas 后加该 div
4. **Tooltip 图例色块加回**: `displayColors: true` + `usePointStyle: true`；`label` 回调按数据集返回「纯拉新/已续秋/转化率」三项（各带对应色块），`afterBody` 追加「未续秋」第四项
### 拉新×续报转化图表：移除失败动态标签方案（本次）

- **问题**: 自定义 `acqLabelManager` 插件与 Chart.js datalabels 产生冲突，导致转化率标签出现在柱子内部、蓝色柱标签被错误渲染成百分比、牵引线/小徽章位置混乱，实际显示效果偏差很大
- **修复**: 彻底移除 `acqLabelManager` 插件和 `acqStyle` 动态样式函数，改为静态可预测布局：
  - 未续秋（蓝色）：数据标签固定在柱内底部，白色 12px/700 粗字
  - 已续秋（绿色）：数据标签固定在柱外顶部，绿色 11px/700 粗字
  - 转化率（橙色）：数据标签固定在折线点上方，与柱体完全独立，橙色 10px/700 加白底徽章
  - 每月拉新总数仍由 `scales.x2` 在 x 轴下方显示（蓝字粗体）
- **校验**: `node --check` 通过；`acqLabelManager`/`acqStyle` 无残留引用


### 拉新×续报转化图表再次调整（本次）

1. **每月拉新总数移到柱子下方**: 去掉图表下方的 `#acq-month-totals` 卡片行；新增 `scales.x2` 辅助 x 轴（position:'bottom'），在每个 x 轴月份标签下方直接显示当月纯拉新总数（如 `105人`、`290科`），蓝色粗体，与柱状图一一对应
2. **蓝色柱改回「未续秋」**: 蓝色数据集 `data` 从 `pureNew` 改回 `notRenewed`，绿色仍为己续秋；堆叠总高 = 未续秋 + 已续秋 = 纯拉新，与 x2 轴总数对齐
3. **移除不喜欢的白字描边**: 蓝色柱内标签恢复为干净白字（12px/700），去掉 `textStrokeColor/textStrokeWidth`；标签仍沉在柱内底部
4. **图例/Tooltip 同步**: 图例改为「未续秋 / 已续秋 / 转化率%」；Tooltip 也按未续秋/已续秋/转化率展示，afterBody 补充纯拉新

### 拉新×续报转化图表紧急修复（本次）

1. **底部总数行未显示**: index.html 中 `#acq-month-totals` div 未写入（JS 里 renderAcqMonthTotals 因取不到元素直接 return）。已补回 div；同时把图表容器高度从 400px 降到 350px（移动端 300px），让总数行能一屏可见
2. **蓝色柱内标签丢失**: 蓝色数据标签原用 `align:'start'`，在 anchor:'start'（柱底）时被摆到柱外下方、被 x 轴/画布裁掉。改为 `align:'top'`，数字显示在柱内底部（anchor:start + align:top = 锚点上方，即柱内）；并加粗到 800、13px、textStroke 增强白字在蓝柱上的可读性
### 拉新×续报转化图表：通用标签防重叠逻辑（本次）

- **问题**: 3月/7月等拉新很少的月份柱段极矮，数据标签在柱内放不下、或被截断/重叠，影响阅读
- **方案**: 新增 `acqLabelManager` 自定义 Chart.js 插件，在 `afterLayout` 计算每个柱段像素高度，供数据标签动态决策
- **实现细节**:
  - 阈值 `18px`：≥18px 标签放柱内（白字）；<18px 标签移出柱外（彩色字+白底小徽章），避免被压缩
  - 阈值 `22px`：≥22px 用 12px 字体；10-22px 用 11px；<10px 用 10px
  - 相邻柱 outside 标签交替水平偏移（偶数柱偏右、奇数柱偏左），降低并排/上下重叠概率
  - 极小柱段（<12px）额外绘制虚线牵引线 + 端点，指向标签
  - 转化率折线标签也随总柱高动态调整：矮柱时 `offset:14px`、`fontSize:9px`，避免与绿标签挤在一起
  - 图表容器高度提升至 420px（桌面）/ 360px（移动），给 outside 标签和 x2 轴总数预留更多空间；`layout.padding.bottom` 增至 54px
- **文件改动**: `uv-trends.js`（插件 + datalabels 回调）+ `uv-charts.css`（高度）
- **设计原则**: 所有动态样式仍使用 2.0 令牌色（蓝#4F6BED/绿#16A34A/橙#E8943B），不引入新颜色；outside 标签用白底半透明边框徽章，保持系统风格

### 拉新×续报转化图表：小柱标签确定性布局 + x2总数位置 + Y轴标题（本次）

- **小柱标签确定性方案**（替代失败的动态插件）：基于数据值阈值，不依赖像素，可预测、无插件冲突
  - 绿色已续秋：永远柱外顶部（绿字 `anchor:'end'/align:'top'/offset:6`）
  - 蓝色未续秋：柱段值 ≥20 → 柱内底部白字；<20（3月/7月）→ 移到基线下方外侧蓝字（`anchor:'start'/align:'bottom'/offset:18`）
  - 写了 `/tmp/validate_acq_labels.js` 几何自检脚本（模拟固定 chartHeight=350、yMax=349 的像素区间）验证 6 列标签**全部无重叠**后才展示
- **x2 总数位置调整**：月份标签 `ticks.padding` 由 8 增至 34（下移到最底）；x2 总数 `padding` 设为 0（紧贴基线下方 = 月份与柱子之间）；`layout.padding.bottom` 改为 48
- **Y轴标题**: `y.title` 显示「拉新总数」（蓝字，`position:'end'` 置于 Y 轴底部）
- **合计标签简化**: x轴合计标签由 `合计 (349人)` 改为仅 `合计`，因蓝色总数已单独体现，避免重复
- JS 语法 `node --check` 通过；`acqLabelManager`/`acqStyle` 已无残留引用

### 拉新×续报转化图表：标签贴近柱子 + Y轴标题与x2总数对齐（本次）

- **Y轴标题对齐**: `y.title.position` 由 `'end'` 改为 `'start'`（移到 Y 轴底部 0 刻度处），`align:'end'`，与 x2 轴蓝色总数处于同一水平区域，避免高高在上的错位感；`layout.padding.left` 由 0 改为 4，给竖排标题留足空间
- **小柱标签贴近柱子**:
  - 蓝色未续秋在柱外时（3月/7月 `<20`）offset 由 18 减至 10，标签紧贴柱底
  - 绿色已续秋柱外标签 offset 由 6 减至 3，标签紧贴柱顶
- 保持确定性布局规则不变：蓝柱 `<20` 移出基线下方、绿柱永远柱外置顶，避免与柱内标签/转化率标签重叠
- JS 语法 `node --check` 通过，服务器 5200 已运行

### 拉新×续报转化图表：「拉新总数」放到 x2 轴左侧（本次）

- **修正理解**: 用户要求「拉新总数」写在底部蓝色总数（如「6人」）的左侧，作为 x2 总数的左侧标题，而不是 Y 轴标题
- **实现**: 移除 `scales.y.title`，改为在 `plugins.acqX2Title.afterDraw` 中于 canvas 内 x2 轴左侧绘制「拉新总数」（蓝字、11px、右对齐、与 x2 tick 同高）
- **位置**: `x = chart.chartArea.left - 8`，`y = x2.y - 8`，确保紧贴首列左侧、与蓝色总数纵向对齐
- JS 语法 `node --check` 通过

### 拉新×续报转化图表：标签不能与 x 轴/底部拉新总数重叠（本次）

- 问题：转化率折线标签（尤其 7 月 5.6%）与底部拉新总数/x 轴标签在垂直方向上过于贴近甚至重叠
- 修改 (`uv-trends.js`):
  - 转化率标签动态 `offset`：≤5%→22, ≤10%→18, ≤20%→14, 其他→10
  - `y1` 轴 `min` 从 0 改为 -10，整体上移折线点
  - 蓝色未续秋小柱（<20）标签基线下方 `offset` 10→14
  - 绿色已续秋柱外标签 `offset` 3→5
  - `layout.padding.bottom` 12→22，给 x2 轴更多空间
  - `x2.ticks.padding` 0→6，拉开与月份标签距离
  - 转化率标签背景透明度 0.88→0.95，边框 alpha 0.35→0.45，字体 10→11
  - 「拉新总数」标题位置 `x2.y - 8` → `x2.y - 4`，与 x2 tick 中线对齐
- JS 语法 `node --check` 通过，服务器 5200 运行中

## 16:51 按新规则重绘趋势图数据标签

用户一次性明确 4 条标签规则后统一调整 (`uv-trends.js`):
1. **蓝色未续报标签**: 默认显示在蓝色柱段内部底部（白字，`anchor='end', align='top', offset=2`）；当 `notRenewed < 12` 时紧贴底部外侧显示（蓝字，`align='bottom', offset=0, padding` 去边距），与基线无竖直留白。
2. **绿色已续报标签**: 默认显示在柱外顶部（绿字，`anchor='end', align='top', offset=4`）；当 `pureNew <= 30 && convRate > 0` 时预判会与转化率标签拥挤，移入柱内顶部（白字 + 黑色 textShadow）。
3. **拉新总数说明**: `layout.padding.left` 4→14，`afterDraw` 中「拉新总数」位置调整为 `chartArea.left - 10, x2.y + 6`，与 x2 蓝色数字纵向居中对齐。
4. **全局防重叠**: 保持转化率动态 offset 与 y1 min=-10，所有标签不遮挡 x 轴/底部月份/蓝色拉新总数；蓝色外部标签仅在矮柱出现且位于 x 轴上方。
- JS 语法 `node --check` 通过，5200 端口服务正常，已刷新预览。

## 17:01 简化标签规则：顶部单标签 + 蓝色柱内居中

用户简化规则后统一调整 (`uv-trends.js`):
1. **顶部只显示一个数字**: 有已续秋时顶部显示绿色数字；已续秋为 0 时顶部显示蓝色未续报数字。
2. **蓝色柱状图标签**: 默认白色、在蓝色段内部居中显示；当蓝色段太矮（<12）且顶部已有绿色数字时，内部蓝色标签隐藏（避免塞不进小柱）。
3. **底部下方不再显示任何标签**。
4. **转化率标签防重叠**: 在原有动态 offset（≤5%→22, ≤10→18, ≤20→14, 其他→10）基础上，当顶部有数字标签时再额外 +6px，避免压到顶部绿色/蓝色标签。
- JS 语法 `node --check` 通过，5200 端口服务正常，已刷新预览。

## 17:12 班级详情弹窗 UI 优化

用户要求优化 2.0 班级详情弹窗，4 项改动：
1. **搭班老师弱化**: 学科 pill 缩小到 18×18px、背景变淡（数 #E8F1FA/#5A8FC9、语 #EAF3E0/#7AA35A、英 #F8EDE0/#C9915A），老师名字颜色改为 `--text-muted`、字重 400；头部与学员行统一使用 `.cd-co-pill` / `.cd-co-name`。
2. **数据卡片加转化率**: `uv-modal.js` 计算 `convRate = shouldRenew > 0 ? round(new_count / shouldRenew * 100) : 0`，卡片从 5 项扩到 6 项（在读/应续/当期转化/已续费/转化率/续报率），CSS 同步调小字号与 padding 保持紧凑。
3. **学员列表分列对齐**: `.cd-student-row` 改为 `grid: 86px 52px 76px 1fr 54px`，对应 姓名/在读情况/续报情况/搭班老师/报前测成绩，各行严格对齐。
4. **报前测成绩**: `repository/__init__.py` 的 `get_class_detail` 从 `manual_json` 读取 `pre_test_score` 并传给前端；`uv-modal.js` 有值时显示 `.cd-score` 标签，无值时该列留空不占位。
- 后端 Python 语法 `py_compile` 通过，前端 JS 语法 `node --check` 通过，5200 端口服务正常，已刷新预览。

## 17:45 班级详情弹窗3项修复

1. **"开课前续报"标签不换行**: `.detail-tag` 增加 `white-space: nowrap; flex-shrink: 0;`，所有标签药丸不再折行。
2. **搭班老师过滤未报名科目**: 学员行和头部搭班老师列表均增加 `.filter(([, teacher]) => t && t !== '-' && t !== '—')`，teacher 为 "-" 或空的科目不显示。
3. **班级ID标签重设计**: 从纯文字 `.cd-class-id` 改为 `.cd-class-id-badge` 药丸标签（带复制图标），放在 `margin-left:auto` 右侧，点击调用 `UVModal.copyClassId()` 通过 `navigator.clipboard.writeText` 复制到剪贴板，复制成功后临时显示"已复制!"提示1.2秒后恢复。
4. **报前测分数功能确认**: 后端已从 `manual_json.pre_test_score` 读取并传给前端，前端有值时显示 `.cd-score` 标签。当前数据库 `manual_json` 只有 channel/purpose/notes/vip_group/renew_willingness 五个字段，没有 pre_test_score 数据。功能已实现但数据为空——需要在台帐校准时将原台帐中的成绩数据导入 `manual_json.pre_test_score` 字段才能生效。
- JS 语法 `node --check` 通过，5200 端口服务正常。

## 17:57 班级详情弹窗对齐校准与班级ID左移

针对截图中个别列不对齐及班级ID易被遮挡的问题，统一调整：
1. **班级ID移到左侧**: `uv-modal.js` 弹窗头部将 `.cd-class-id-badge` 放在 `.subj-pill` 与主讲姓名之前，避免右侧被截断；`uv-components.css` 移除 `margin-left:auto`，并给 class ID 增加 `flex-shrink:0`。
2. **增加列表表头**: 学员列表上方新增 `.cd-student-list-thead`（姓名/在读/续报/搭班老师/前测），与数据行使用相同 grid 列宽，建立对齐基准。
3. **标签列严格对齐**: `.cd-col-status .detail-tag` 和 `.cd-col-renew .detail-tag` 设置 `width:100%; justify-content:center; margin:0; box-sizing:border-box`，让状态/续报药丸在单元格内撑满并居中，消除不同文字宽度导致的视觉错位。
4. **搭班老师网格对齐**: 头部和学员行的每个搭班科目用 `.cd-co-item` 包裹，内部使用 `grid-template-columns: 18px 1fr` 固定学科 pill 与姓名分列，名字设置 `min-width:30px` 和 `white-space:nowrap`。
5. **头部换行安全**: `.cd-header-top` 增加 `flex-wrap: wrap`，主讲姓名过长时自动折行，class ID 与学科 pill 不会挤出容器。
- JS 语法 `node --check` 通过，5200 端口服务正常。

## UV Dashboard 2.0 — 班级详情弹窗微调（#326）

### 变更内容
1. **班级ID移到老师姓名右侧**: `.cd-header-top` 顺序改为 学科pill → 主讲姓名 → 班级ID徽章（原先班级ID在最左侧）
2. **去掉前测列**: 学员列表从5列减为4列（姓名/在读/续报/搭班老师），grid-template-columns 从 `86px 52px 76px 1fr 54px` 改为 `86px 52px 76px 1fr`，移除 scoreVal/scoreHtml 相关 JS 代码
3. **CSS清理**: `.cd-student-list-thead` 和 `.cd-student-row` 同步更新为4列；`.cd-col-score` 的 text-align 规则移除
4. JS 语法 `node --check` 通过，5200 端口服务正常

## UV Dashboard 2.0 — 学员明细卡片布局调整（#327）

### 变更内容
1. **顾问 + 三科老师同一行**: `_renderStudentDetail` 中新增 `.sd-staff-row`，把 `data.advisor` 和 `data.co_teachers` 合并成一行显示；移除原来的单独顾问行和独立的 `sd-co-teachers` 区块
2. **卡片紧凑化一屏展示**:
   - 弹窗 `.modal-content` padding 从 `20px 24px` 减到 `16px 20px`，`max-height` 从 `80vh` 提高到 `95vh`
   - `.sd-header` 间距和底部边距减少
   - `.sd-summary` gap 从 10px 减到 8px，item padding 减到 6px 8px，数字从 18px 减到 16px
   - 学科卡片 `.sc-content` padding 从 `10px 12px` 减到 `8px 10px`，gap 从 8px 减到 6px
   - 学科 pill 从 22x22 减到 20x20，字号 9px；学科名 12px
   - 状态横幅 padding 5px 6px，字号 11px，icon 14x14
   - `.sc-section` padding 6px 8px，gap 3px
   - `.kv-row` 字号 11px，行高 1.3，`.kv-key` 宽度 34px
3. JS 语法 `node --check` 通过，5200 端口服务正常

## UV Dashboard 2.0 — Bug修复：学员详情弹窗数据不显示（#328）

### 问题根因
- #327重构顾问+三科老师同行布局时，替换了旧的 `coTeacherHtml` 代码块，但新代码中 `coEntries` 变量未定义仍被引用（第240行）
- 导致 `ReferenceError: coEntries is not defined`，整个 `_renderStudentDetail()` 函数崩溃，学员详情弹窗无法渲染

### 修复
- 在 `uv-modal.js` 第239行补充 `const coEntries = Object.entries(coTeachers);`
- JS 语法验证通过，5200端口服务正常

## UV Dashboard 2.0 — 排班班级卡片视觉重构（#329）

### 变更内容
1. **移除左侧竖杠**: `.schedule-class-card::before` 从左侧竖条改为顶部 4px 渐变色条，hover 时加宽到 5px，更现代高级
2. **头部信息重组**: 学科圆形图标 + 老师姓名作为左侧主标题；班级ID、时间·教室·教学点合并为一行内联次要信息
3. **期次/班型固定色块**: 期次做成学科色实心白字 pill，班型做成学科色 15% 淡底实线边框 pill，两个色块置顶右，视觉突出
4. **班级数据区**: 保留三列统计（在读/续报/续报率），修复原 `.renew-num` 选择器错误，续报数字现在正确显示绿色
5. 涉及文件：`uv-dashboard-2.0/static/js/uv-app.js`、`uv-dashboard-2.0/static/css/uv-components.css`
6. JS 语法 `node --check` 通过，5200 端口服务正常

## UV Dashboard 2.0 — 班级卡片色块统一（#330）

### 变更内容
1. **班型色块统一**: `.cls-type-pill` 去掉 inline `subjColor`，改为 CSS 统一定义 — 靛蓝色 `#6366F1`（淡底15%+实线边框30%+文字色）
2. **教学点色块新增**: 从 `cls-meta-inline` 内联文本中提取 `teaching_point`，新增 `.cls-pt-pill` 色块 — 青色 `#0D9488`（淡底+边框+文字色），与班型色块并列放在右侧标签列
3. 右侧标签列现在3个pill: 期次(学科色实心) → 班型(靛蓝淡底) → 教学点(青色淡底)
4. 内联文本精简为 `时间 · 教室`
5. 涉及文件: `uv-app.js`、`uv-components.css`
6. JS 语法验证通过，5200端口服务正常

## UV Dashboard 2.0 — 教学点色块位置调整（#331）

### 变更内容
- 教学点色块(`.cls-pt-pill`)从右侧标签列移到左侧副标题行，与班级ID、时间·教室同行排列
- 右侧标签列精简为2个pill: 期次 + 班型，减少竖排行数
- 副标题行顺序: 班级ID → 教学点色块 → 时间·教室

## UV Dashboard 2.0 — 班级ID位置调整（#332）

### 变更内容
- 班级ID从副标题行移到标题行，放在老师姓名后面
- 副标题行精简为: 教学点色块 + 时间·教室，整体左对齐
- 标题行顺序: 学科pill → 老师姓名 → 班级ID

## UV Dashboard 2.0 — 期次/班型/教学点色块统一配色（#333）

### 需求
三个色块用色区分、统一，不再跟随学科色变化。

### 实现
- `uv-config.js` 新增三套固定调色板（独立于 SUBJECT_COLOR）：
  - `PERIOD_COLORS`: 零期紫#7C3AED / 一期蓝#2563EB / 二期青#0891B2 / 三期绿#059669（实心白字）
  - `CLASS_TYPE_COLORS`: 潜能班橙 / 启能班玫红（淡底描边 {bg,border,text}）
  - `TEACHING_POINT_COLORS`: 天成大厦琥珀 / 同创汇绿 / 新宝利蓝（淡底描边）
  - 兜底：`TINT_FALLBACK_PALETTE` 按字符串哈希稳定取色，未知值也保持一致
  - 辅助方法：`periodColor()` / `classTypeColor()` / `teachingPointColor()`
- `uv-app.js`：期次 pill 改用 `periodColor`，班型/教学点 pill 内联应用 `bg/border/text`
- `uv-components.css`：移除 `.cls-type-pill`/`.cls-pt-pill` 固定颜色，仅保留布局
- JS 语法验证通过，5200端口服务正常

## UV Dashboard 2.0 — 色块配色迭代（高级和谐版 #334）

### 问题
#333 的彩虹色太花、不和谐。

### 新方案（分温度家族 + 低饱和）
- **期次（实心白字，冷色渐变序列）**：零期靛#4F46E5 → 一期天蓝#0EA5E9 → 二期青#0D9488 → 三期翠绿#059669（类比冷色，和谐序列）
- **班型（暖色淡底描边）**：潜能班琥珀 / 启能班玫红
- **教学点（紫色家族淡底描边）**：天成大厦紫#6D28D9 / 同创汇靛紫#5B21B6 / 新宝利中性slate
- 三个维度靠色温区分（冷/暖/紫），每个维度内部和谐；兜底 TINT_FALLBACK_PALETTE 改为低饱和和谐色
- 刷新后查看效果，待用户反馈

## UV Dashboard 2.0 — 启能班配色升级为高级档（#335）

### 变更
- 用户认可 #334 整体配色和谐，但要求 启能班（更高级班型）视觉更高级
- `CLASS_TYPE_COLORS.启能班` 由玫红淡底改为 品牌紫→靛蓝实心渐变（linear-gradient(135deg,#7C3AED,#4F46E5) + 白字），与潜能班柔和琥珀淡底形成"高级档/常规档"层级
- 仅改 uv-config.js，刷新查看

## UV Dashboard 2.0 — 主讲看板/顾问看板回归「数据明细」（#336）

### 需求
在 2.0 系统「数据明细」下加回主讲看板与顾问看板：左侧栏排名（复用数据看板人员排行口径），右侧详情（主讲含班级+学员，顾问含名下学员），功能结构参考 1.0 系统，视觉自行迭代。

### 后端（repository/__init__.py + api/__init__.py）
- `_split_periods(period)`：逗号分隔 → 列表，空返回 []（全期次）
- **重写 `get_teacher_detail`**：修复期次过滤 bug（`period not in p_str` 子串判断 → `period_list and not any(p in plist for p in period_list)` 集合判断，兼容逗号分隔期次）；新增班级维度聚合（classes 列表含 class_id/subject/period/time_slot/class_type/teacher/active/renewed/pre/new/renewal_rate/should/conv_rate，按 class_id 排序）；新增 summary 汇总（total_active/total_renewed/total_pre/total_new/total_should/renewal_rate/conv_rate/class_count）；学员增加 renewal_class 字段
- **新增 `get_advisor_detail`**：UV/PV 双维度统计（uv_active/uv_renewed/uv_pre/uv_new/uv_should/uv_renewal_rate/uv_conv_rate + 对应 pv_ 字段 + student_count）；学员输出 subjects 列表（subject/period/class_id/teacher/status/continue_fall/renewal_class），is_renewed
- `api_overview_teacher_detail` / `api_overview_advisor_detail` 改为接受并转发 period/teaching_point 筛选参数
- py_compile 通过

### 前端
- `index.html`：`#tab-detail` 新增「主讲看板」(`data-subtab="teacher"`)/「顾问看板」(`data-subtab="advisor"`) 两个 sub-tab；新增 `#sub-teacher`(`.board-layout` → `.board-sidebar` 含排行切换 + `#teacher-list-content` + `.board-detail` `#teacher-detail-content`)、`#sub-advisor`(含 `#advisor-list-content` + `#advisor-detail-content`)
- `uv-app.js`：`switchSubTab` 新增 `if(tab==='teacher') UVTeacher.load(); if(tab==='advisor') UVAdvisor.load();`
- `uv-teacher.js`（重写）：`_params()` 复用 `UVFilters.buildParams('dashboard')`（与数据看板排行口径一致）；`viewDetail` 渲染 board-summary 7格汇总条 + class-card-grid 班级卡片（点击开 UVModal.openClassDetail）+ 学员情况表格（点击行开 UVModal.openStudentDetail，事件委托）；新增 `_ptOfClass()`
- `uv-advisor.js`（重写）：`_params()` 同上；`viewDetail` 调 `UVApi.getAdvisorDetail(name, params)`；渲染主维度汇总条（按 uvMode 显示 UV/PV）+ 副维度对照条（board-summary-secondary）+ 名下学员表格（按学员聚合学科，每科续秋徽章 pre/new/none，点击行开 UVModal.openStudentDetail）；事件委托
- `uv-api.js`：`getAdvisorDetail(name, params={})` 接受并转发 period/teaching_point
- `uv-components.css`：追加 `.board-layout`(flex左右分栏 height calc(100vh-230px)) / `.board-sidebar`(348px) / `.board-detail` / `.board-summary`(grid 7列) + `.bs-val.rate-high/mid/low/accent` / `.board-summary-secondary` / `.class-card-grid` / `.class-card`(`.cc-bar`学科色条) / `.board-student-table` / `.stu-subj-pill` / `.badge-renew.none/pre/new/high` / `.no-data` / 响应式 `@media (max-width:980px)`

### 验证
- JS `node --check` 全通过；Python `py_compile` 通过
- 服务重启（PID 93554，5200端口）后 API 实测：
  - `/api/overview/teacher/陈中帅` → keys=[classes,students,summary,teacher,total]，summary.class_count=7, total_active=144, renewal_rate=34.0%, conv_rate=10.4%（与数据看板排行数字 100% 对齐）
  - `/api/overview/advisor/戎凯01` → summary 含 uv_active=43/pv_active=119/student_count=43 等正确字段
  - 期次过滤：零期→58条，零期,一期→92条且全在 {零期,一期}，bug 修复确认
- index.html 接线（两个 sub-tab + 容器）全部存在；静态 JS/CSS HTTP 200

## UV Dashboard 2.0 — 数据明细 5 项 UI 迭代（#355-#359）

### 1. 多选筛选器点选逻辑修复（#355）
- 根因：`uv-filters.js` 的 `onMultiSelectChange` 在 `selectedSet.size === 0` 时统一走「反选」（保留其他选项），但用户先点「全选」取消全选后，空 Set 也命中此分支 → 点击单项变成反选，不符合习惯
- 修复：进入时先捕获 `wasDeselectedAll = this._deselectedAll.has(id)`；若 `wasDeselectedAll && size===0` → 仅选中该项（常规单选）；其余分支（全选态取消单项 / 取消已选项 / 追加后满选归全）保持不变
- 验证：`node --check` 通过

### 2. 学员检索搜索框拉伸（#356）
- `index.html` 搜索组 `.sf-group-search` 内联样式 `margin-left:auto;min-width:220px` → `flex:1;min-width:220px;margin-left:auto`，占满筛选栏剩余宽度

### 3. 学员检索点击出详情卡片（#357）
- 根因：`uv-student.js` 学员卡片 `onclick` 调 `UVStudent.viewDetail`，该函数渲染 `.student-detail-card`（未定义样式）导致弹窗空白/无详情
- 修复：卡片改为事件委托 `container.onclick` → `UVModal.openStudentDetail(uid)`，复用高级学员详情弹窗；删除 `UVStudent.viewDetail` 与 `UVStudent.closeModal`（实际关闭走 `UVModal.closeModal()`）

### 4. 主讲/顾问吸顶窄条重设计（#358）
- 之前 `.board-detail-sticky` 把「标题 + 7格汇总条」全吸顶，占空间且滚动时数据穿过顶部
- 改为：吸顶区只保留窄条 `.board-detail-head`（姓名 + 右侧 3 个关键指标：续报率/转化率/在读），汇总条 `.board-summary` 移出吸顶区进入常规流
- CSS：`.board-detail-sticky` 收窄 padding、移除底部 margin；新增 `.is-stuck` 阴影（`box-shadow: 0 6px 18px rgba(26,26,46,.10)`）；新增 `.bdh-metrics`/`.bdh-metric`
- 滚动阴影联动：teacher/advisor `viewDetail` 末尾 `container.onscroll` 切换 `is-stuck`（`scrollTop > 4`）
- 背景 `var(--bg-panel)` 不透明 + z-index:20 杜绝穿模

### 5. 统一学员列表卡片（续报/在读/退费 #359）
- 三个看板（主讲/顾问/学员检索）学员卡片右侧统一双 pill 状态块：续报状态 + 在读/退费状态，配色统一直观
- `uv-config.js` 新增 4 个 helper：`renewalState(continueFall, renewalClass)`（未续报/开课前续/当期转化/已续报）、`mergeRenewal(states)`（多科取最高优先级）、`enrollState(statuses)`（任一含「退」→ 已退费，否则在读）、`statusPillsHTML(renewal, enroll)`
- CSS 新增 `.stu-status`/`stu-pill` + `.pill-renew-{none,pre,new,done}` + `.pill-enroll-{active,refund}`（带前置圆点）
- 主讲卡：单科记录 `renewalState(s.continue_fall, s.renewal_class)` + `enrollState([s.status])`
- 顾问卡/检索卡：遍历 `subjects` 用 `mergeRenewal` + `enrollState` 聚合
- 配色：未续报灰 / 开课前续靛蓝 / 当期转化·已续报绿 / 在读蓝 / 已退费红

### 验证
- 5 个 JS 文件 `node --check` 全通过；服务器 5200 重启后 index.html 与静态 JS 均 HTTP 200
- 注意：`UVStudent.viewDetail`/`closeModal` 已无引用；`badge-renew` 旧样式保留但不再用于卡片

## UV Dashboard 2.0 — 复刻 1.0 分享卡片功能（#367-#369）

### 需求
在 2.0 数据看板「人员排行」旁加「分享卡片」按钮，全面复刻 1.0 数据总览下的分享卡片：筛选器、展示信息、数据逻辑完全复刻；下载卡片 UI 迭代为 2.0 高级风格。

### 后端：重写 `/api/share-card`（api/__init__.py:295）
- **关键决策**：2.0 原 `/api/share-card` 走 `engine.compute()`（与 1.0 口径不同），已整体替换为**直接遍历 `student_snapshots` 的 1.0 逻辑移植**（用 `get_latest_students()` + `config.is_continue_fall`/`classify_renewal_by_pay_time`/`auto_detect_period`）
- 2.0 `subjects_json` 单科结构与 1.0 完全一致（class_id/teacher/room/time_slot/class_type/period/status/continue_fall/fall_pay_time），故逻辑可 1:1 移植
- 返回结构严格保持 1.0 形状：`kpi`(active_pv/uv/renewal_rate/renewal_pv/renewal_uv/renewal_uv_rate/refund_pv/class_count) / `by_point` / `by_subject` / `by_teacher` / `by_class`(仅选老师时) / `by_advisor`(UV+PV 双维度，pre/new 用 fall_pay_time 判定) / `advisor_current_period` / `filters` / `available_teachers|points|advisors` / `run_time`
- 导入 `is_continue_fall, classify_renewal_by_pay_time` 到 api 模块

### 前端：新增 `static/js/uv-share-card.js`（独立模块，全局函数 openShareCard/closeShareCard/renderShareFilters/setShareFilter/toggleShareMetric/setShareAdvisorMode/updateShareCard/renderShareCard/downloadShareCard）
- 调 `UVApi.getShareCard(params)`（非 1.0 的全局 `api()`）；期次选项取自 `UVConfig.PERIODS`
- 数据逻辑完全复用后端返回值；卡片 UI 重做：白色圆角卡 + 品牌色 `#4F6BED` 区块标题竖条 + KPI 浅底色+左侧色条小卡 + 表格化分教学点/分学科/老师/顾问 + 顾问 UV/PV 切换
- 下载：html2canvas@1.4.1（CDN，新增到 index.html `<head>`），`downloadShareCard()` → PNG

### 接线
- index.html：「人员排行」header 加 `<button class="btn btn-sm btn-share" onclick="openShareCard()">分享卡片</button>`
- index.html：新增 `#shareOverlay` 弹窗（`.share-modal` 结构：`.share-modal-header` + `#shareFilters` + `#shareCardPreview` + 下载按钮）
- index.html：`<head>` 加 html2canvas；`</body>` 前加 `<script src="/static/js/uv-share-card.js">`
- uv-components.css：新增 `#shareOverlay`/`.share-modal`/`.share-chip`/`.sc-card` 系列样式（2.0 premium 风格，卡片内联样式保证 html2canvas 可捕获）

### 验证
- Python `py_compile` 通过；JS `node --check` 通过
- 服务 5200 重启后：`/api/share-card` HTTP 200，返回 1.0 形状数据
  - 无筛选：KPI active_pv=1089/uv=401/renewal_rate=13.7%/renewal_uv_rate=20.9%/refund_pv=99/class_count=54；by_point/by_subject/by_teacher/by_advisor 齐全；by_class=[]（未选老师）
  - 期次=零期：KPI 收窄、advisor_current_period=零期、顾问 pre/new 重新判定（单期模式）
  - 老师筛选：by_class 返回 4 条班级、by_teacher 仅 1 条（前端据此隐藏老师排行表）
- index.html 含按钮/弹窗/脚本引用；uv-share-card.js / uv-components.css 均 HTTP 200
