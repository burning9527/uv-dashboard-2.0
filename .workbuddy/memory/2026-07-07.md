# 2026-07-07 工作日志

## 修复分享页面三处问题

- **顶部KPI随期次联动**：后端 `api_share_generate` 新增 `kpis_by_period`（每期次最后一天的 active/refund/renew/renewal_rate），模板注入 `KPIS_BY_PERIOD` 变量；前端 `renderKPIs()` 根据 `currentPeriod` 自动切换显示。
- **图表标签不显示/重叠**：重写 `dataLabelPlugin`：统一收集段内标签、柱顶标签、折线标签后按优先级碰撞解决，折线点优先上方、冲突则翻转下方或垂直偏移，仍冲突才跳过；首末/最大/最小/密度填充保证关键标签可见。
- **云端外链旧数据**：重新部署最新分享页面到 `https://6f70c70e6e22442395746a05f0b01d6e.app.codebuddy.work`；`api_share_generate` 改为优先读取 `share_dir/.deploy_url`，再回退全局缓存，避免返回与当前分享页无关的旧外链。
- 关键文件：`uv-dashboard/app.py`, `uv-dashboard/templates/share_page.html`, `uv-dashboard/templates/index.html`。

## 修复 dataLabelPlugin 导致分享页无数据 + 看板标签颜色不匹配

- **根因**：重写 dataLabelPlugin 时，share_page.html 将其定义为 `const` 对象，但三个图表函数仍用 `plugins:[dataLabelPlugin()]` 调用为函数 → JS 报错 "not a function"，所有图表均无法渲染。
- **修复 share_page.html**：将 `dataLabelPlugin()` 改为 `dataLabelPlugin`（对象引用），段内标签 textColor 从 `#E0E3EB`（亮色）改为 `#0F1119`（暗色主题下白底芯片上的深色文字）。
- **修复 index.html**：段内标签 textColor 从 `#E0E3EB` 改回 `#2D3142`（亮色主题深色文字），折线标签 fillColor 从 `rgba(22,25,35,0.92)`（暗色）改为 `rgba(255,255,255,0.92)`（亮色），段内 fillColor 从 0.85 改为 0.9。
- **云端重新部署**：新外链 `https://6551b1101653490e8b89646c44402850.app.codebuddy.work`，已验证无 dataLabelPlugin() 调用、数据变量完整。
- **教训**：两套页面（亮色看板 vs 暗色分享页）共用插件逻辑时，标签颜色必须分别适配主题，不能一套颜色走天下。

## 修复分享页外链数据不全 + 期次 UV 续报率为 0 + 部署自动化

- **外链只有零期数据**：`api_share_generate` 原先用 `periods_filter` 限制全部数据计算，导致分享页只含筛选期次。
  - 修复：保存用户选择的 `selected_periods` 仅用于 `default_period`（默认 Tab）和 `filter_text`；数据计算改用空的 `periods_filter`，`trend_data` 和 `kpis_by_period` 始终包含全部期次。
- **期次 UV 续报率为 0**：`kpis_by_period[p]['renewal_uv_rate']` 原先硬编码为 0。
  - 修复：主循环中新增 `uv_by_period` / `renewed_uv_by_period` 集合，按期次统计在读 UV 和续秋 UV，再计算每期次的 UV 续报率。
- **部署到云端未自动落实**：原先 `/api/share/deploy` 仅写入请求文件，需要人工调用 `workbuddy_cloudstudio_deploy`。
  - 修复：创建 WorkBuddy 自动化 `UV分享页面自动部署`（automation-1783393318493），每小时巡检 `.deploy_requests`，自动部署 pending 请求并回写 `share_dir/.deploy_url`。
  - 前端 `cloudDeploy()` 改为提交后显示"已提交自动部署，系统将在下次巡检时完成（通常 1 小时内）"，并延长轮询至 65 分钟。
- **新外链**：`https://b22cdf9cfbed4b45853f509e61cdc197.app.codebuddy.work`（含全部 5 个期次、三个趋势图表、按期次 UV 续报率）。

## 分享页顾问排行改为转化排行

- **标题**：share_page.html 中 `<h3>顾问续报排行</h3>` 改为 `<h3>顾问转化排行</h3>`。
- **转化率公式**：`convRate = new_renewed / should_renew * 100`（当期转化 ÷ 应续），替代原先 `total_renewed / active * 100`（总续报率）。
- **颜色阈值**：`convRateColor()` 替代 `advRateColor()`，≥25% 为 high(绿)、≥15% 为 mid(琥珀)、<15% 为 low(红)，与主讲续报率阈值对齐。
- **排序逻辑**：后端 `advisor_list_raw.sort(key=lambda x: x['conv_rate'])` 替代 `x['new_renewed']`；按期次 `advisors_by_period` 同步改为 `x['conv_rate']`；`api_overview_advisor_list` 和 `api_share_card` 的 `advisor_list.sort` 同步改为 `x['renewal_rate']`。

## 统一排行卡片界面 + 切换按钮迭代

- **核心改动**：顾问和讲师排行不再有两套卡片界面，统一使用转化卡片的4字段布局（在读、应续、当期转化、已续费），续费/转化切换只改右上角百分数和排序。
- **续费模式**：右上角显示续报率（已续费/在读），按续报率降序排列
- **转化模式**：右上角显示转化率（当期转化/应续），按转化率降序排列
- **讲师学科标签**：改为名字同行后面显示（`tch-subj-inline`），不再单独一行
- **颜色阈值统一**：≥25% high(绿)、≥15% mid(琥珀)、<15% low(红)
- **切换按钮**：从 `.sub-tab` 改为 `.rank-toggle`（圆角胶囊分段控件），背景色区分活跃/非活跃状态，带box-shadow选中态
- **CSS统一**：新增 `.rank-stat-row` / `.rank-stat-cell` / `.rs-label` / `.rs-val` 代替旧的 `.adv-stat-row` / `.asc-label` / `.asc-val`
- **分享页面**：同步改CSS（暗色主题适配）和JS（统一渲染逻辑），讲师数据后端新增 `subjects` 字段
- **后端**：`by_teacher_pv` 增加 `subjects` 集合追踪，`by_teacher_period` 同步，`teacher_list` 输出增加 `subjects` 字段

## 14:55 修复UV台帐看板JS语法错误

- **根因**：`renderTeacherList()` 函数在 `}).join('');` 行后缺少闭合 `}`，导致JS解析器报 "Unexpected end of input"，所有按钮事件处理器失效。
- **修复**：在2588行添加 `}` 闭合函数体，删除遗留注释行。
- **重启问题**：原Flask用系统Python3.9启动（缺flask模块），改用managed Python 3.13.12重启后生效。
- **验证**：`node --check` 语法通过，API `/api/overview/stats` 返回正常数据（total_uv: 430）。

## 12:12 自动化: UV分享页面部署

处理4个pending部署请求: 1成功(b1de72edda3c → `https://0e35cedb3ef54cd684bdeb65af0736dd.app.codebuddy.work`), 3失败(fbddf1ae17dd/1462ab8b3631/d60090326ab5, 均为CloudStudio fetch failed, 疑似限流)

## 10:30 业务趋势板块排行榜替换完成 + 字段映射修复

- **替换"分教学点在读PV"视窗**：用主讲+顾问排行榜双卡片替换原chartByPoint图表，包含续费/转化切换按钮
- **字段映射修复**：Teacher API返回`active_count`/`renewed_count`/`conv_rate`而非前端期望的`active`/`renewal_rate`
  - 排序：续费模式用`renewed_count/active_count*100`，转化模式用`conv_rate`
  - 显示：`activeCount`替代`t.active`，计算值替代`t.renewal_rate`
  - Advisor API字段(`active`/`renewal_rate`)与渲染函数匹配，无需修改
- **State变量完整**：`trendsTeacherRankMode`/`trendsAdvisorRankMode`/`trendsTeacherRankData`/`trendsAdvisorRankData`均已声明
- **JS语法验证通过**，Flask重启成功(5200端口)，浏览器测试页面渲染正常

## 16:50 学科标签移到姓名前 + 顾问教学点简称标签（第一版）

- **学科标签位置**：主讲排行中学科标签从姓名后移到姓名前（3处：index.html趋势排行卡、index.html主讲列表、share_page.html分享页排行）
- **顾问教学点简称**：顾问姓名前加教学点简称pill（同学科标签样式 `.tch-subj-inline`+`.tch-subj-pill`）
  - 映射：陈亮延/王达04/沈思浩/龙倩敏→天，戎凯01/王丽/苏丽婷→同，杨林33/林志豪01/张敏烨→宝，其余→教
  - `ADVISOR_POINT_TAG` 常量添加到 index.html 和 share_page.html
- JS语法验证通过，Flask重启(5200端口)

## 16:57 统一顾问教学点pill样式 + 业务数据板块应用 + 调整间距

- **统一UI**: 顾问教学点标签改为与主讲学科标签一致的pill样式（`.tch-subj-pill`），按教学点区分颜色：天→#f5a623(琥珀)、同→#1d9e75(绿)、宝→#378add(蓝)、教→#6b7280(灰)
- **应用位置**: 已应用到 index.html 业务数据顾问列表（`renderAdvisorList`）、index.html 业务趋势顾问排行（`renderTrendsAdvisorRanking`）、share_page.html 分享页顾问排行
- **间距优化**: `.tch-subj-inline` 从 `margin-left:6px` 改为 `margin-right:8px`，标签与姓名间距更清晰
- **辅助函数**: 新增 `advisorPointPill(name)` 统一生成标签HTML，避免重复代码
- JS语法验证通过，Flask重启(5200端口)

## 17:13 修复业务数据顾问列表姓名居中

- **问题根因**: `renderAdvisorList` 的 `.adv-top` 使用 `justify-content: space-between`，且标签、姓名、百分比是3个独立 flex item → 姓名被夹在中间
- **修复**: 与主讲 `.tch-top` 结构对齐，将教学点标签和顾问姓名包在同一个 `<span class="adv-name">` 内，百分比单独在右侧
  - `adv-top` 内只有两个 flex item：`adv-name`（含标签+姓名）和 `adv-rate`（百分比）
  - 视觉上标签+姓名作为一个整体靠左，百分比靠右，姓名不再居中
- JS语法验证通过，Flask重启成功(5200端口)

## 17:24 顾问排行增加 UV/PV 筛选器

- **分享页** (`share_page.html`)：
  - 新增状态变量 `advisorRankUVMode = 'uv'`
  - 在顾问排行标题右侧、续费/转化切换按钮旁增加 UV/PV 切换按钮组
  - `renderAdvisorRanking` 根据 `isPV` 读取 `pv_active`/`pv_should_renew`/`pv_new_renewed`/`pv_total_renewed`
  - 标题自动追加 `(UV)` / `(PV)`，排序与数据同步切换
- **业务趋势页** (`index.html`)：
  - 新增状态变量 `trendsAdvisorRankUVMode = 'uv'`
  - 业务趋势板块顾问排行同样增加 UV/PV 切换按钮组
  - `renderTrendsAdvisorRanking` 使用 `pick()` 统一提取 UV/PV 字段，避免重复逻辑
- **切换函数隔离**：续费/转化按钮选择器改为 `[data-mode]`，UV/PV 按钮选择器用 `[data-uv]`，避免互相干扰
- JS语法验证通过，Flask重启成功(5200端口)

## 17:34 修复分享页面顾问PV数据为空

- **根因**：分享页面的顾问数据有三处缺失PV字段：
  1. `advisor_list_raw`（整体顾问排行）：只输出UV维度字段，缺少 `pv_active`/`pv_pre_renewed`/`pv_should_renew`/`pv_new_renewed`/`pv_total_renewed`
  2. `advisors_by_period`（按期次顾问排行）：`by_advisor_period` 只跟踪PV计数，没有UV数据，也没有PV前缀字段
  3. `advisor_stats` 的PV计数器从未被递增（只有初始化 `pv: {active: 0, pre: 0, new: 0}`，但没有 `+=1` 操作）
- **修复**：
  1. `advisor_list_raw`：从 `sets['pv']` 读取并输出PV维度5个字段
  2. `by_advisor_period`：初始化时增加 `uv_active`/`uv_pre`/`uv_new` set()；每条记录同时更新PV计数和UV set
  3. `advisors_by_period`：UV字段从set长度取值，PV字段从计数器取值
  4. 主循环增加 `uid_active_pv_count` 变量；顾问聚合处递增PV计数器
- **验证**：整体顾问PV active=119/UV=43（戎凯01），按期次顾问均包含PV字段

## 17:48 修复零期退费UV+在读UV≠学员总数UV

- **现象**：零期学员总数UV=131，在读UV=119，退费UV=16，119+16=135≠131，差4人。
- **根因**：`/api/overview/stats` 中整体退费UV用 `total - active_uv` 计算（保证与在读UV相加等于总数），但**按期次**的 `refund_uv_by_period` 直接取 `period_refund_uv` 集合（含所有含退费科目的学员），未剔除同时有在读科目的学员，导致按期的在读与退费UV重复计数。
- **具体重叠学员（零期）**：
  - 4830601122 黄思睿：雪球思维在读，悦读创作/双语素养课前退
  - 4634094018 黄芷瑶：雪球思维课前退，悦读创作/双语素养在读
  - 4787927529 姚诗琳：雪球思维在读，悦读创作/双语素养课前退
  - 4815971458 李奕萱：雪球思维课前退，双语素养在读
- **修复**：`app.py` 605行将 `period_refund_uv_out` 改为 `period_total_uv - period_active_uv`，与整体逻辑一致，保证每期次 `active_uv + refund_uv = total_uv`。
- **验证**：修复后零期 119+12=131；一期、二期、三期也保持相等。Flask 5200端口重启成功，API返回正常。

## 18:00 重构退费UV期次归属逻辑

- **问题**：之前修复零期退费UV后，四期退费UV总和=36≠整体退费UV=25，全退学员被跨期重复计数（同一学员在多个期次的退费集合中都出现）。
- **新逻辑**（用户指定）：
  1. 退费UV仅计入"三科均不在读"的学员（全退学员）
  2. 退费UV的期次归属 = 该学员最后退费科目的所在期次（最晚期次优先）
  3. 例：学员零期悦读创作退+二期双语素养退 → 归属二期退费
- **实现**：`api_overview_stats()` 在主循环后增加二次遍历，对每名全退学员收集所有退费期次，取 `max(period_order.index)` 确定归属期次；`period_refund_uv_out` 改用新集合。
- **验证**：
  - 整体：total=430, active=405, refund=25 ✓（active+refund=total）
  - 按期：零期10 + 一期9 + 二期1 + 三期5 = 25 = 整体退费UV ✓
  - 跨期归属案例：梁跃曦(零期+二期→归属二期)、陈韵蓉(二期+三期→归属三期)、小诺(一期+二期+三期→归属三期)
- **语义变化**：单期 `在读UV + 退费UV` 不再等于 `学员总数UV`，因为部分全退学员被归属到其他期次；但四期总和 = 整体退费UV。

## 21:14 UV Dashboard 2.0 前端完善（配色统一 + 分享页面）

- **统一UI配色系统**：CSS :root 变量定义品牌色（--c-primary）、学科身份色（数学蓝/语文绿/英语橙）、状态色（续报率≥30%绿/≥15%黄/<15%红）、顾问标签色（天/同/宝/教）、中性色阶（4级灰阶+阴影+圆角）；JS 端 UVConfig.COLORS 与 CSS 变量一一对应，图表颜色全部引用此常量。
- **修复前端Bug**：
  - `uv-matrix.js` `_totalHTML()` 中 `Math.round(x, 1)` 第二参数无效 → 改为 `(x).toFixed(1)`
  - 讲师/顾问排行 `onclick` 传字符串名字 → 改用 `data-name` + `this.dataset.name`
  - 学员检索筛选器下拉从硬编码 → `UVStudent.fillDropdowns()` 动态填充
  - 补充 `schedule-class-card` CSS、`file-label`/`calibrate-*` CSS class
- **筛选器联动**：所有板块的 select onchange 先调用 `syncAndReload()` 更新 UVFilters.state，再触发数据加载；初始化时渲染期次 chips。
- **分享页面模板** (`share_page.html`)：内联 CSS（复用主面板配色变量）、Chart.js 趋势图表、KPI 卡片、主讲/顾问排行（含续报/转化切换+UV/PV切换）、JS 内嵌数据（stats_json/teachers_json/advisors_json）。
- **API更新**：`api_share_generate` 改为注入 JSON 数据而非 Python 对象；`api_overview_stats` 新增 `run_time` 字段；`by_teacher` 新增 `should_renew` 字段。
- **已完成任务**：#179(验证渲染)、#180(筛选器联动)、#181(数据渲染细节)、#182(CSS配色统一)、#183(分享页面模板)。
- 关键文件：4个CSS、10个JS、1个HTML模板、1个分享页面模板、api/__init__.py、engine/__init__.py。

- **代码改动**：
  1. `app.py`：添加 `_get_data_dir()` 函数区分开发/打包模式数据目录（打包用 `~/Library/Application Support/UV Dashboard/`）
  2. `app.py`：`Flask()` 初始化添加 `template_folder` 指向 `sys._MEIPASS/templates`（打包模式）
  3. `app.py`：启动逻辑添加 `webbrowser.open()` 自动打开浏览器
  4. `db.py`：添加 `set_data_dir()` 函数，移除模块级 `init_db()` 调用（由 app.py 在 set_data_dir 后调用）
  5. `app.py`：`SHARES_FOLDER` 改为从 `DATA_DIR` 取路径
- **打包**：PyInstaller 6.21.0，spec 文件 `uv_dashboard.spec`，bundle identifier `com.uv.dashboard`
  - 包含 templates 目录、所有依赖（Flask/openpyxl/pandas/numpy等）
  - 排除不需要的包（tkinter/matplotlib/scipy/pyngrok等）
  - 自定义图标 AppIcon.icns（Pillow生成，蓝色圆角矩形 + UV文字）
  - Info.plist 移除 LSBackgroundOnly（保持终端窗口可见）
- **产出**：29MB DMG 安装包 → `/Users/wangboning/Downloads/UV台帐管理系统_Installer.dmg`
  - 包含：UV Dashboard.app + Applications 链接 + 使用说明.txt
  - 测试：打包模式启动成功，Flask 在5200端口响应正常，页面渲染正确

## 16:14 完成需求5+6：排名卡片字段统一 + 顾问标签改全称小字

- **需求5：排名卡片字段统一**：
  - 修复 `renderAdvisorList` 中顾问卡片标签"已续费"→"已转化"（index.html L3135），与 `renderTrendsAdvisorRanking` 保持一致
  - 最终统一：主讲卡片显示 在读/应续/当期转化/已续费；顾问卡片显示 在读/应续/当期转化/已转化
  - 切换续报/转化模式时 stat-row 数据不变，只变右上角百分数和排序（此逻辑之前已正确实现）

- **需求6：顾问标签改全称小字**：
  - **后端**：`api_overview_advisor_list()` 新增 `teaching_point` 字段——从 `advisor_students` 中取学员数最多的教学点作为顾问主要教学点；`api_share_generate()` 新增 `advisor_tp` 跟踪字典，同步添加到 `advisor_list_raw` 和 `advisors_by_period`
  - **前端 index.html**：删除 `ADVISOR_POINT_TAG` 硬编码映射和 point-tian/tong/bao/jiao CSS；新增 `.adv-point-tag` 样式（10px灰色小标签，亮色主题 #F0F1F5/#8C92A3）；`advisorPointPill(name, teachingPoint)` 改为数据驱动
  - **前端 share_page.html**：同上（暗色主题 #1E2233/#8C92A3）
  - 教学点全称：天成大厦/同创汇/新宝利（从DB动态获取，旧的天/同/宝1字简称已废弃）
  - 适配70-80个教学点扩展：无需前端硬编码，后端自动返回

## 23:51 修复顾问标签间距 + 当前期次绿色圆圈

- **需求1：顾问标签与姓名间距**：
  - 问题：顾问排行和顾问列表中 `advisorPointPill` 直接拼接姓名，缺少 `margin-right`，与主讲格式不一致
  - 修复：将 `advisorPointPill(a.name)` 包裹在 `<span class="tch-subj-inline">` 中（与主讲 `subjPills` 一致），利用 `.tch-subj-inline{margin-right:8px}` 提供间距
  - 涉及：index.html（趋势排行 `renderTrendsAdvisorRanking` + 业务数据列表 `renderAdvisorList`）、share_page.html（顾问排行）

- **需求2：当前期次绿色圆圈标记**：
  - 问题：`/api/overview/stats` 不返回 `period_config`，导致 `currentPeriod` 始终为空，绿色圆圈 CSS（`.period-row.current .period-label`）未生效
  - 修复：新增前端 `getAutoPeriod()` 函数，按月日判断当前期次（与后端 `get_auto_period()` 逻辑一致：≥629零期、≥713一期、≥727二期、≥810三期）
  - `currentPeriod` 赋值改为 `pc.auto_period || getAutoPeriod() || ''`
  - CSS 已有绿色圆圈样式（border-color:#15803D;background:#15803D），无需修改
  - 当前7/7对应零期，"零"字将显示为绿色圆圈

## 15:20 修复1.0系统三处问题（期次标签/分享链接/顾问标签）

- **期次标签显示"期"**：`p.slice(-1)` 取末字"期"→改为 `p[0]` 取首字"零/一/二/三"
- **分享链接500错误**：`api_share_generate` 引用已移除的 `advisor_main_tp` 变量→删除该行
- **顾问标签恢复1字简称**：恢复 `ADVISOR_POINT_TAG` 映射（天/同/宝/教）+ `ADVISOR_TAG_CLASS` CSS类，`advisorPointPill(name)` 改回1参数
- 涉及文件：index.html、share_page.html、app.py、db.py

## 23:01 2.0系统筛选器全多选改造

- **需求**：用户反馈筛选器多选功能不方便，要求全部8项筛选器开放多选，界面操作更方便
- **改造路径**（5个文件）：
  1. `config/__init__.py`：FILTER_CONFIG 中 year/branch/grade/season/teaching_point 的 type 从 `'select'` 改为 `'multi_select'`，period 从 `'chips'` 改为 `'multi_select'`，advisor/enrollment_status/renewal_status 同步改为 `'multi_select'`
  2. `templates/index.html`：将6个 `<select>` / `<div class="sf-chips">` 全部改为 `<div class="multi-select">` div结构（8项看板筛选器均为多选）
  3. `static/js/uv-filters.js`：全面重写
     - state.dashboard 8项全部改为 Set 类型（空Set=全选）
     - `_msOptionsMap` 新增 ms-year/ms-branch/ms-grade/ms-season/ms-period/ms-tp 6项映射
     - `_msStateMap` 新增ID→字段映射
     - `renderMultiSelect` 增加搜索框（选项>5时显示）
     - `toggleAllMultiSelect/onMultiSelectChange` 逻辑简化：空Set=全选，部分→全选=clear()，全选→toggle=clear()
     - `buildParams` 8项全部用 Set→comma-separated 逻辑
     - `resetSection` dashboard分支全部 clear()+恢复默认值到Set
     - 保留 `renderPeriodChips`（排班看板仍用chips）
  4. `static/js/uv-app.js`：
     - `onDashboardFilterChange` 简化为只调 `loadDashboard()`（多选handler已直接更新state，无需从DOM读取）
     - `resetDashboardFilters` 改为调 `UVFilters.resetSection('dashboard')` + `renderAllDropdowns()`
     - 移除 `onDashboardPeriodClick`（期次不再用chips）
     - `initApp` 移除select默认值设置和dashboard-period-chips渲染
  5. `static/css/uv-components.css`：增强多选样式
     - 搜索框 `.ms-search/.ms-search-input`
     - 下拉箭头 `.ms-text::after`
     - 选中标记 `.ms-text.has-selection`
     - checkbox accent-color
     - 看板筛选栏紧凑样式
- **验证**：JS语法检查通过，API正常返回(total_uv=431)，Flask重启成功
