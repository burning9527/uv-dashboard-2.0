# 2026-07-11

## 提取防遮挡标签插件并接入 2.0 图表（Task #374/#375）

- 新建 `static/js/uv-chart-labels.js`：从 1.0 分享页 `dataLabelPlugin` 抽取为可复用模块 `UVChartLabels.dataLabelPlugin`，主题化（light/dark）。支持柱内段标签、柱顶标签、折线标签碰撞避让。
- `templates/index.html`：移除 `chartjs-plugin-datalabels@2.2.0` CDN，改为加载 `/static/js/uv-chart-labels.js`。
- `static/js/uv-trends.js`：移除 `Chart.register(ChartDataLabels)` 与所有 `datalabels:` 配置；主趋势图 + 拉新月度图改为 `plugins:[UVChartLabels.dataLabelPlugin]` + `options.plugins.uvDataLabels={theme:'light',fontFamily:FONT_FAMILY}`。
- `UVTrends.generateShare()`：改为发送 1.0 形态参数（subjects/teaching_points/periods/teachers，Set→逗号串，空=全选）。
- 验证：node --check 通过；服务重启后 `/api/share/generate` 返回 share_id，`/share-view/<id>` 200，生成的 HTML 含 5 个 canvas + 月度图表 + 嵌入 dataLabelPlugin；MONTHLY_DATA 含 5 个月数据。无运行时错误。
